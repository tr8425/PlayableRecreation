# 리얼리티 모드 — 명세

> 상태: **기획**. 아직 한 줄도 만들지 않았다.
> 계기: ferny(창작마당, 2026-09-09) — 검토 기록은 `Feedback/FEEDBACK.md` 의 같은 날 항목.
> 제약은 `HANDOFF.md` §2 를 따른다. 이 문서는 그것을 다시 쓰지 않고 §14 에서 점검만 한다.
> 읽기 좋은 판: <https://claude.ai/code/artifact/2f4e50e1-6be6-4db6-acaa-ffb569bbad49>
> (내용은 같다. **정본은 이 파일이다** — 고칠 때는 여기를 먼저 고친다.)

---

## 0. 한 줄

**상대를 식민지 사람 중에서 고르고, 둘을 실제로 판 앞에 세운다.**

AI 는 손대지 않는다. 리얼리티 모드가 하는 일은 기존 AI 에게 **이름과 몸을 주는 것**이다.
이 한 문장이 이 기능이 감당 가능한 크기인 이유다.

## 1. 왜

> "I really wish you actually had to select one of the other colonists to be your
> opponent. It feels currently like it sorta reverses the immersion it brings to
> the table by having you play against invisible AIs and not actual people in
> your colony" — ferny

맞는 지적이다. 이 모드는 "식민자가 하던 것을 직접 한다"로 몰입을 만들어 놓고,
막상 앉으면 맞은편이 `Opponent (Good)` 라는 난이도 이름뿐이다. 판은 진짜 물건인데
상대만 진짜가 아니다.

## 2. 하지 않는 것

명세는 안 하는 것을 적어야 커지지 않는다.

- **멀티플레이가 아니다.** 사람 대 사람은 이 문서의 범위 밖이다.
- **AI 를 새로 만들지 않는다.** 상대 폰은 기존 난이도 AI 의 얼굴이다.
  체스 AI 가 그 폰의 성격·특성을 반영하지 않는다.
- **대화를 만들지 않는다. 다만 보여는 준다.** 무슨 말을 할지는 바닐라가 정한다.
  우리는 그것을 창 안으로 끌어올 뿐이다(§7.2). 게임을 아는 대사를 쓰는 것은 다른 기능이다.
- **일시정지와 같이 켜지 않는다.** → D1.
- **판 위의 말을 폰이 실제로 옮기지 않는다.** 애니메이션·이펙트는 범위 밖이다.

## 3. 결정 (다시 뒤집지 않기로 한 것)

### D1. 리얼리티 모드와 `pauseWhilePlaying` 은 함께 켜지지 않는다

멈춰 두면 리얼리티 모드가 하려던 것이 전부 같이 멈춘다. 습격이 올 수 없으니
"체스를 두다 전투에 나간다"가 사라지고, 시간이 안 흐르니 두 폰이 대화할 일도
사교 다툼이 날 일도 없다. **리얼리티 모드는 시간이 흐르는 것이 요점이다.**

설정에서 리얼리티 모드를 켜면 `pauseWhilePlaying` 은 꺼지고, 왜 그런지 한 줄로 말한다.
둘 다 켜진 세이브를 읽을 때는 리얼리티 모드가 이긴다.

### D2. 난이도는 정해 주는 값이 아니라 **제안값**이다

상대를 고르면 그 사람의 스킬로 난이도를 미리 짚어 준다. 그러나 플레이어가 바꿀 수 있다.

정해 주는 쪽으로 가면 전적 장부가 식민지 구성에 잠긴다 — `GameRecord` 도 숙련도도
난이도 칸으로 쌓이는데, 걸작 칸을 열려면 걸작급 식민자가 있어야 한다. 뉴럴 트레이너
한 방이나 적성 유전자 하나로 칸이 통째로 바뀌기도 한다. ferny 가 지적한 것은
**상대에게 얼굴이 없다는 것**이지 난이도를 뺏어 달라는 것이 아니다.

### D3. 판의 난이도는 시작할 때 얼어붙는다

지금 이미 그렇게 되어 있다 — `GameSession.tier` 가 세이브에 박히고, 이어 하기 생성자는
`game.ClampTier(resumed.tier)` 로 그 값을 **다시 읽는다.** 리얼리티 모드가 이걸
"상대의 지금 실력으로 다시 계산"으로 고치면 안 된다. 두던 판이 도중에 어려워지는 것은
"창을 다시 열어도 결과가 다시 굴러가지 않는다"는 약속을 깨는 일이다.

`ClampTier` 를 거치는 것도 그대로 둔다. 난이도 단계 수를 나중에 줄여도 옛 판이
터지지 않는 이유가 그 한 줄이다.

### D4. 배고픔·피로는 멈춤이 아니라 **자리를 뜨는 것**으로 푼다

붙잡는 토일에 탈출구를 단다. 배가 고프면 일어나 밥을 먹으러 가고, 두던 판은
`saveSessions` 가 이미 가구에 남겨 준다. 돌아와서 이어 둔다.

> **정정.** `saveSessions` 는 켤 수 있는 설정이고 끄면 `Dialog_MiniGame.SaveSession` 이
> 창을 닫을 때 세션을 지운다. 그러면 "밥 먹고 와서 이어 둔다"가 성립하지 않는다.
> **리얼리티 모드에서는 세션 저장이 선택이 아니다** — 켜져 있어야 하고, 꺼져 있으면
> 리얼리티 모드를 켤 수 없거나 켜는 순간 함께 켜져야 한다. §12 에서 정한다.

우리가 가진 기능이 그대로 답이 되고, 이야기로도 이쪽이 낫다 — 저녁 먹으러 판을
비우는 두 사람.

### D5. 상대가 사라진 판은 되살리지 않는다

죽었든 납치됐든 캐러밴을 탔든, 그 판은 끝난 판이다. 상대를 갈아 끼워 이어 하는 길은
만들지 않는다. 반쯤 둔 판은 그 상대와의 그 판이다.

### D6. 대화는 바닐라가 굴리고, **창이 그것을 보여 준다**

`Pawn_InteractionsTracker` 가 가까이 있는 폰끼리 알아서 굴린다는 가정 위에 선다.
**이 가정은 §16 에서 확인한다.** 틀리면 이 항목은 통째로 빠지고, 리얼리티 모드는
"상대에게 얼굴이 생긴다"까지만 한다 — 그것만으로도 기능은 성립한다.

> **범위 정정 — 방문객에게는 해당하지 않는다.** `TryInteractRandomly` 는 후보를
> `pawn.Map.mapPawns.SpawnedPawnsInFaction(pawn.Faction)` 에서만 뽑는다. 다른 팩션의
> 방문객은 판을 사이에 두고 마주 앉아도 **서로의 자연 대화 후보가 아니다.**
> 그래서 §11 의 방문객 대전에서 D6 은 굴러가지 않는다 — 그쪽은 18.1 의 명시적
> `TryInteractWith` 가 유일한 통로다. 자연 대화는 **같은 팩션 참가자에게만** 기대한다.

**다만 굴러도 플레이어는 못 본다.** 말풍선은 지도 위에 뜨는데 플레이어는 판을 보고
있다. 폰 둘을 붙잡아 두는 값을 치르고 아무것도 안 보이면 2단계는 뜻이 없다.
그래서 바닐라가 만든 말을 **창 안으로 끌어온다**(§7.2). 만드는 것이 아니라 옮기는 것이다.

게임을 아는 **대사**("그 수는 좀…")는 여기 포함되지 않는다 — 아홉 게임 × 두 언어의
글쓰기 과제다. 다만 **"게임 상태를 모른다"는 것은 이 들여오는 방향에만 해당한다.**
반대로 내보내는 상황 서술은 게임을 알아야 뜻이 있고, 그건 대사가 아니라 데이터라서
제약 4 와 부딪히지 않는다 — 18.1 에서 정리했다.

---

## 4. 진입 흐름

```
식민자 선택 → 가구 우클릭
  │
  ├─ 리얼리티 모드 꺼짐 → (지금과 같다)
  │
  └─ 리얼리티 모드 켜짐 · 승부 게임
        │
        1. 상대 고르기 (FloatMenu — 자격 있는 식민자·방문객, §10)
        │     자격 없는 사람은 이유와 함께 회색으로 (바쁨 / 못 감 / 능력)
        2. 난이도 창 — 고른 상대의 실력으로 짚어 준 칸이 미리 선택됨 (D2)
        3. 두 사람에게 Job 을 준다
        │     주도한 쪽: PR_GoToGame  (걸어감 → 창 열기 → 붙잡기)
        │     상대  쪽: PR_JoinGame   (걸어감 → 붙잡기)
        4. 둘 다 도착하면 창이 열린다
        │     한쪽이 못 오면 판을 열지 않고 한 줄로 알린다
        5. 판이 끝나거나 창이 닫히면 두 Job 이 함께 끝난다
```

**진입점은 이 하나가 아니다.** 지금 판을 여는 길이 세 갈래고, 나머지 둘은 이 흐름을
통째로 건너뛴다.

| 진입점 | 지금 하는 일 | 리얼리티 모드에서 |
|---|---|---|
| 폰 우클릭 → FloatMenu | `GameEntry.Begin(game, board, selPawn)` | 위 흐름 그대로 |
| **가구만 선택 → 기즈모** | `GameEntry.Begin(game, parent, null)` — **폰이 없다** | 폰 없이 앉힐 수 없다. 자리를 채울 사람을 고르게 하거나 기즈모를 접는다 |
| **저장된 판 이어 두기** | `GameEntry.Resume(session)` 직접 호출 | 걷기·붙잡기를 건너뛴다. **재개도 같은 준비 절차를 타야 한다** |

세 갈래가 한 문으로 모이지 않으면 리얼리티 모드는 "켰는데 어떤 길로는 그냥 열린다"가
된다. **모든 진입을 같은 참가자 준비 절차로 모은다.**

**4번의 "둘 다 도착"이 이 흐름의 유일한 새 동기화 지점이다.** 주도한 쪽이 먼저 닿으면
상대를 기다린다. 기다리는 동안 창은 열지 않는다 — 빈 의자를 보여 줄 이유가 없다.

## 5. 데이터 모델

### 5.1 `GameSession`

```csharp
public Pawn opponentPawn;          // 리얼리티 모드가 아니면 null
Scribe_References.Look(ref opponentPawn, "opponentPawn");
```

`GameSession` 은 `*SaveData` 가 아니므로 클래스 이름 제약(제약 7)에 걸리지 않는다.
필드 하나를 더하는 것은 세이브 호환을 깨지 않는다 — 옛 세이브는 null 로 읽힌다.

`seatedPawn` 의 뜻은 바뀌지 않는다: **플레이어가 조작하는 쪽**이다.

### 5.2 새 Def

| 종류 | defName | 쓰임 |
|---|---|---|
| `JobDef` | `PR_JoinGame` | 상대를 판 앞으로 부르고 붙잡아 둔다 |
| `ThoughtDef` | `PR_OpponentGone` | 상대가 사라진 판 앞에 다시 선 사람의 기분 |
| `ThoughtDef` | `PR_PlayedTogether` | (선택) 둘이 한 판을 끝냈을 때 |

전부 우리 Def 다. 바닐라 Def 는 건드리지 않는다(제약 2).

### 5.3 새 `InvalidationReason`

`OpponentGone` 하나만 더한다. 죽음·납치·추방·캐러밴·맵 이탈·적대화를 값 여럿으로
쪼개지 않는다 — 번역 키만 늘고 플레이어가 얻는 것은 같다. 문구에서 누구였는지만 말한다.

**다운은 여기 넣지 않는다.** 다운된 폰은 `Spawned` 이고 `Dead` 가 아니며 같은 맵에 있다 —
§8 의 판정식에 걸리지 않는다. 넣을지 말지를 정해야 하는데, **넣지 않는 쪽으로 정한다.**
다운은 사라진 것이 아니라 자리를 뜬 것이고(D4), 일어나면 이어 둘 수 있어야 한다.
대신 Job 은 끊긴다 — 판은 가구 위에 남고 상대는 병상에 있다.

## 6. Job — 붙잡는 토일

### 6.1 예약

지금 `TryMakePreToilReservations` 는 아무것도 예약하지 않고 `true` 를 돌려준다.
Job 이 즉시 끝나므로 지금은 무해하지만, 붙잡기 시작하면 같은 가구를 두 판이 물거나
상대로 지목된 폰이 다른 판에도 불려 간다.

```csharp
return pawn.Reserve(job.targetA, job, maxPawns: 2, stackCount: 0, errorOnFailed: errorOnFailed)
    && pawn.Reserve(opponent, job, maxPawns: 1, stackCount: 0, errorOnFailed: errorOnFailed);
```

`maxPawns: 2` 는 바닐라가 2인용 가구에 쓰는 관용이다. 예약은 다른 모드와 부딪히기 쉬운
자리이므로 실패를 조용히 받아 "지금은 쓸 수 없다"로 끝낸다.

**가구만 예약해서는 상대 중복을 못 막는다.** 보드가 둘이면 각각 두 자리를 따로 잡으므로,
같은 폰을 두 판의 상대로 지목하는 것이 예약으로 걸리지 않는다. 두 번째 지목이 첫 판의
Job 을 끊는 모양으로 나타난다. **상대 폰 자체를 예약한다.** 그리고 두 Job 중 하나라도
시작에 실패하면 **양쪽을 함께 되돌린다** — 한 사람만 걸어가다 서 있는 그림을 만들지 않는다.

> **`ReservationManager` 의 규칙.** 같은 대상·같은 레이어에 이미 예약이 있고 그
> `MaxPawns` 가 지금 요청과 다르면 **개수와 상관없이 즉시 거절된다.** 같아도 이미
> `maxPawns` 만큼 차 있으면 거절이다. 이 규칙이 18.3 의 관전과 정면으로 부딪힌다 —
> 거기서 다시 다룬다.

### 6.2 토일 구성

```
Toils_Goto.GotoThing(TargetIndex.A, PathEndMode.Touch)
FaceTarget
[주도한 쪽만] 상대가 닿을 때까지 대기 → 창 열기
Hold  (ToilCompleteMode.Never)
```

### 6.3 `Hold` 의 종료 조건

| 조건 | 결과 | 비고 |
|---|---|---|
| 판이 끝났다 / 창이 닫혔다 | `Succeeded` | 정상 |
| 배고픔이 임계 아래 | `InterruptForced` | **D4. 이게 굶주림의 답이다** |
| 피로가 임계 아래 | `InterruptForced` | 〃 |
| 상대의 Job 이 끝났다 | `Succeeded` | 혼자 남지 않는다 |
| 가구가 사라짐·금지됨 | `Incompletable` | `FailOnDespawnedNullOrForbidden` |
| 징집·정신 붕괴·다운 | (바닐라가 처리) | Job 이 알아서 끊긴다 |

임계값은 설정으로 빼지 않는다. 바닐라 여가 Job 이 쓰는 값에 맞춘다.

여가(joy)는 붙잡혀 있는 동안 쌓되, **여가가 가득 찼다고 판을 끊지 않는다.**
바닐라 여가 Job 은 가득 차면 끝나지만 여기서는 두던 판이 우선이다.

## 7. 창이 폰을 비추는 방식

### 7.1 결합 — 폰이 자리를 뜨면 창도 닫힌다

지금은 둘이 무관하다 — Job 이 즉시 끝나므로 창만 살아 있다. 붙잡기 시작하면
"폰은 일어섰는데 창은 열려 있다"를 정의해야 한다.

**어느 쪽이든 자리를 뜨면 창이 스스로 닫히고 판은 가구에 남는다.** 위협 인터럽트가
이미 쓰는 문법 그대로다. 왜 닫혔는지 한 줄로 말한다("X 가 밥을 먹으러 일어났다").

이때 기권으로 치지 않는다 — `RecordResult` 는 호출하지 않는다. 중단이지 패배가 아니다.

### 7.2 대화를 창 안으로

바닐라 사교 상호작용은 `Find.PlayLog` 에 `PlayLogEntry_Interaction` 으로 쌓인다.
두 폰에 걸린 것만 골라(`LogEntry.Concerns`) 창의 기록 패널에 올린다. 문구는
`ToGameStringFromPOV(seatedPawn)` 이 만들어 준다. Harmony 불필요.

**자리는 이미 있다.** 기록 패널은 프레임워크가 그리고(`Dialog_MiniGame.DrawLog`)
게임이 `MiniGameWorker.Log` 로 줄을 댄다. 사교 줄은 게임이 아니라 **프레임워크가 따로
들고 같은 패널 안에 구분된 띠로 그린다** — 그래야 게임 코드가 한 줄도 안 바뀐다(제약 4).

시간순으로 섞지 않는다. `Log` 는 그냥 문자열 목록이라 시각이 없고, 아홉 게임의 `Log` 를
타임스탬프 있는 구조로 바꾸는 값에 비해 얻는 것이 적다.

**패널이 없는 게임은 애초에 둘이 할 수 없는 게임이다.** `showLog` 가 꺼진 셋은
별 보기·슬롯·펀칭백인데, 앞의 둘은 승부가 없고 펀칭백은 1×1 가구다(§16-4).
그래서 이 설계에 빈칸이 남지 않는다.

폴링은 위협 감시와 같은 잣대로 둔다(0.5초). 매 프레임 `PlayLog` 를 훑을 이유가 없다.

**커서가 필요하다.** `PlayLog` 는 새 항목을 앞(0번)에 꽂고 150개만 남긴다. 커서 없이
훑으면 창을 열 때 옛 대화가 쏟아지고, 0.5초 사이에 여러 항목이 생기면 순서가 엉킨다.

- 창을 연 시점의 마지막 항목을 세션별로 기억하고, **그 뒤에 생긴 것만** 소비한다.
- **양쪽 참가자를 모두 `Concerns` 로 갖는 항목만** 집는다. 옆을 지나가는 사람의
  잡담까지 판 안으로 들어오면 안 된다.
- 150개를 넘겨 밀려난 항목은 그냥 잃는다. 되살리지 않는다.

## 8. 무효화 — 폰이라는 새 축

`Invalidation.Check` 는 지금 전부 가구만 본다(파괴·이동·손상·수리·전투·청소·만료).
`seatedPawn` 은 죽었는지조차 묻지 않는다. 리얼리티 모드는 여기에 **축을 하나 더한다.**

```
참가자 중 누구든 null · Dead · Destroyed · !Spawned
  · Map != board.Map · HostileTo(player)   →  참가자 소멸
```

**상대만 보지 않는다.** 지금 `Invalidation` 은 `seatedPawn` 이 죽었는지조차 묻지 않는다.
상대 쪽만 새로 검사하면 **플레이어 쪽이 죽은 판을 이어 둘 수 있다** — 그 뒤 머리글·전적·
두 사람 보상이 전부 없는 사람을 다룬다. 축은 "상대"가 아니라 **"참가자"** 로 잡는다.

이유 값은 그래도 하나다(`OpponentGone`). 문구에서 누가 없는지만 말한다.
`invalidateOnOpponentGone` 설정 하나를 붙이되 기본은 켬으로 둔다.

**검사 시점은 바꾸지 않는다.** "두는 중인 판은 검사하지 않는다. 흐트러졌는지는 언제나
돌아왔을 때 알게 된다"는 기존 철학 그대로다 — B 가 판 앞에 다시 섰을 때 비로소
상대가 없다는 것을 안다. 사용자가 그린 장면과 정확히 같다.

그때: 토스트 한 줄 + `PR_OpponentGone` 생각 + 세션 폐기.

## 9. 보상은 누가 받나

| 무엇 | 누구 | 근거 |
|---|---|---|
| 전적 (`GameRecord`) | 판 하나에 한 번 | 난이도는 여전히 플레이어가 고른 칸이다 (D2) |
| 숙련도 (`Mastery`) | 지금과 같이 게임·난이도 단위 | 폰 단위가 아니다 |
| 여가 | **둘 다** | 둘 다 앉아 있었다 |
| 생각 | 둘 다 (`PR_PlayedTogether`) | 선택 항목 |

> **"둘 다"는 지금 저절로 되지 않는다.** 여가·숙련 보너스를 주는
> `MapComponent_Recreation` 의 스캐너는 `MiniGameDef.vanillaJob` 으로만 표를 만든다
> (`ByJob`). `PR_GoToGame` · `PR_JoinGame` 은 그 표에 없으므로 **참가자는 이 경로로
> 아무것도 받지 못한다.** 붙잡는 토일을 만든다고 여가가 저절로 오르지도 않는다 —
> 바닐라 여가 Job 이 아니기 때문이다.
> **두 JobDriver 의 `Hold` 틱에서 여가 획득을 직접 한다.** 방문객에게도 줄지, 숙련도
> 보너스까지 줄지는 §11 · §15 에서 따로 정한다.

**전적에 상대 이름을 남길지는 1단계에서 정하지 않는다.** 남기면 그 사람이 자란 뒤
기록의 뜻이 흐려지는 문제를 같이 풀어야 한다(그때 몇 칸이었는지도 같이 남겨야 한다).
1단계는 `GameRecord` 를 건드리지 않는다.

## 10. 상대 자격

| 대상 | 되는가 | 비고 |
|---|---|---|
| 식민자 | ○ | 기본 |
| 아이 (생명공학) | ○ | 막을 이유가 없다 |
| 방문객 | ○ | §11 |
| 죄수·노예 | **범위 밖** | 저항 감소·설득과 얽히면 다른 기능이 된다 |
| 동물·메카노이드 | × | |
| 자기 자신 | × | 목록에서 뺀다 |
| 이미 다른 판에 앉은 폰 | × | 예약으로 막힌다 |

능력 검사: 의식, 조작(말을 옮긴다), 시각(판을 본다). 게임마다 무엇이 필요한지는
`MiniGameDef` 가 이미 `linkedSkill` 을 들고 있으니 그 옆에 두는 것이 자연스럽다.

거리: 도달 가능(`CanReach`) 검사는 필수. 거리 상한도 둔다 — 없으면 맵 반대편 사람을
두 시간 기다리는 것이 게임이 된다.

## 11. 방문객과 우호도

한 판을 끝까지 두면 그 방문객의 팩션 우호도가 조금 오른다
(`Faction.TryAffectGoodwillWith`, Harmony 불필요).

**상한이 없으면 체스로 동맹을 산다.** 방문 1회당 한 번, 작은 값으로 묶는다.
방문객이 체류 시간이 끝나 먼저 일어서는 경우는 §8 과 같은 처리에 문구만 다르게 한다.

이건 3단계다. 1단계에서는 방문객을 상대로 고를 수만 있게 하고 우호도는 뒤로 미룬다.

## 12. 설정

| 항목 | 기본 | 비고 |
|---|---|---|
| `realityMode` | 꺼짐 | 켜면 `pauseWhilePlaying` 과 상호 배타 (D1) |
| `invalidateOnOpponentGone` | 켬 | 무효화 목록에 한 줄 |
| `realityGoodwill` | 켬 | 3단계 |

새 설정은 셋을 넘기지 않는다. 나머지는 전부 코드가 정한다.

**`saveSessions` 와의 관계를 정한다.** D4 의 "밥 먹고 와서 이어 둔다"는 세션이 남아
있어야만 성립한다. 그런데 `saveSessions` 는 끌 수 있고, 끄면 창을 닫을 때 세션이 지워진다.
→ **`realityMode` 를 켜려면 `saveSessions` 가 켜져 있어야 한다.** 꺼져 있으면 리얼리티
모드 항목을 회색으로 두고 이유를 한 줄로 말한다. 몰래 켜 주지 않는다 — 플레이어가 끈
설정을 모드가 되돌리면 그게 더 나쁘다.

## 13. 번역 키 (EN·KO 양쪽)

대략 12~16 짝. `verify.py` 가 짝과 미사용을 잡는다.

```
PR.Reality.PickOpponent      상대 고르기
PR.Reality.Waiting           X 를 기다리는 중
PR.Reality.NoOpponent        올 수 있는 사람이 없다
PR.Reality.CannotPlay        X 는 지금 앉을 수 없다 (이유)
PR.Reality.LeftForNeed       X 가 일어섰다 (배고픔·피로)
PR.Reality.Header            맞은편 이름표
PR.Reality.Talk              기록 패널의 사교 띠 제목
PR.Invalidate.OpponentGone   무효화 문구
Thought_PR_OpponentGone      생각 이름·설명
Thought_PR_PlayedTogether    〃
```

## 14. 제약 점검 (`HANDOFF.md` §2)

| 제약 | 이 명세는 |
|---|---|
| 1. Harmony 0 개 | 지킨다. 새 JobDef·JobDriver·기존 Comp 확장뿐이다 |
| 2. 바닐라 Def 수정 없음 | 지킨다. 새 Def 는 전부 우리 것 |
| 3. `Core/`·`AI/` 는 Verse 를 모른다 | **건드리지 않는다.** 이 기능은 전부 `Framework/` 다 |
| 4. `Framework/` 는 게임을 모른다 | 지킨다. 상대 폰은 게임이 아니라 수명의 개념이다 |
| 5. 게임 속도를 건드리지 않는다 | **D1 이 이 제약에서 나왔다.** `CurTimeSpeed` 를 읽지도 쓰지도 않는다 |
| 6. `packageId` 불변 | 무관 |
| 7. `*SaveData` 이름 불변 | 지킨다. `GameSession` 은 `*SaveData` 가 아니다 |

**제약 3 이 이 기능의 크기를 정한다.** 상대 폰은 규칙이나 AI 가 아니라 수명에 속하므로
게임 코드는 한 줄도 바뀌지 않는다. 아홉 게임 전부가 공짜로 리얼리티 모드를 얻는다.

## 15. 단계

| 단계 | 내용 | 이게 끝나면 |
|---|---|---|
| **1** | 상대 고르기 + 머리글 이름 + 제안 난이도. **붙잡지 않는다**<br>· 세 진입점을 한 문으로 모으기 (§4)<br>· 상대 폰 예약 + 실패 시 양쪽 되돌리기 (§6.1) | ferny 의 지적이 해결된다. 굶주림 문제가 아예 없다 |
| **2** | 두 폰 붙잡기 — 예약, `Hold` 토일, 욕구 탈출구, 창·폰 결합 | "실제로 앉아 있다"가 된다 |
| **3** | `OpponentGone` + 생각. 방문객 우호도 | 이야기가 닫힌다 |
| **4** | 관계 반영, 마주 보게 세우기 같은 다듬기 | |

**1단계만으로도 낼 수 있다.** 그게 이 순서의 요점이다 — 2단계가 늦어져도 1단계가
사용자에게 닿는다. 반대로 1단계 없이 2단계는 뜻이 없다.

1단계에 딸린 두 줄은 §19 에서 올라온 것이다. 나중에 붙일 수 있는 것이 아니다 —
기즈모로 열면 상대 고르기를 건너뛰고, 상대를 예약하지 않으면 같은 사람이 두 판에
지목된다. 둘 다 **1단계의 약속을 직접 깨는 구멍**이라 같은 단계에 있어야 한다.

## 16. 확인해야 할 가정 (인게임)

만들기 전에 테스트 식민지에서 봐야 하는 것들. 여기서 틀리면 명세가 바뀐다.

1. **바닐라 사교 상호작용이 정말 알아서 굴러가는가** (D6). 두 폰을 오래 붙여 두면
   대화가 뜨고 모욕이 쌓이면 사교 다툼이 나는가. 그리고 그것이 `Find.PlayLog` 에
   `PlayLogEntry_Interaction` 으로 남는가 — 남아야 창으로 끌어올 수 있다(§7.2).
2. **`SkillRecord.Level` 이 유전자 적성을 포함하는가.** 포함이면 제노젬 한 번에
   제안 난이도가 두 칸 뛴다 (D2 라서 사고는 아니지만 문구가 어색해진다).
3. **`maxPawns: 2` 예약이 바닐라 여가 가구에서 통하는가.** 그리고 그 가구를 쓰는
   다른 모드의 Job 과 부딪히지 않는가 — `MaxPawns` 가 다르면 코드상 즉시 거절이다.
4. **가구 앞에 두 폰을 세울 자리가 있는가.** 마주 보게 하는 것은 나중 문제지만,
   1×1 가구(펀칭백)는 애초에 2인이 성립하지 않는다 — `MiniGameDef` 에
   "둘이 할 수 있는 게임인가"를 두어야 할 수도 있다.
5. **사교 다툼이 났을 때** 판을 무효화할지(그림은 좋다) 그냥 둘지.
6. **붙잡는 동안 대화 빈도가 볼 만한가.** 코드상 `Hold` 토일의 기본 `socialMode` 는
   `Normal` 이라 같은 팩션끼리는 가능하지만, **얼마나 자주 뜨는지와 좌석별 시야**는
   돌려 봐야 안다. 너무 뜸하면 §7.2 의 창이 대부분 비어 있게 된다 — 그러면 18.1 의
   내보내기가 선택이 아니라 필수가 된다.

## 17. 리스크

- **콜로니가 선다.** 여섯을 세 탁자에 붙잡아 두면 일이 멈춘다. 상한을 둘지, 아니면
  플레이어의 문제로 둘지. → 2단계에서 정한다.
- **예약 충돌.** 다른 모드가 같은 가구를 예약하면 조용히 실패해야 한다.
- **1×1 가구.** 펀칭백·슬롯은 2인이 성립하지 않는다. `hasMatch` 와 별개로
  "2인 가능" 표시가 필요할 수 있다(§16-4).
- **기능이 커 보인다.** 1단계만 내면 작다. 2단계부터가 진짜 비용이다.

## 18. 다음 층 — 아직 결정하지 않은 확장

셋 다 §0~§17 을 **바꾸지 않고 그 위에 얹는다.** 크기는 서로 다르다 — 18.2 는 이 기능을
"두 사람이 앉는다"에서 **"여럿이 모인다"** 로 바꾸는 큰 얹기고, 18.1 과 18.3 은 붙잡는
사람을 늘리지 않는 작은 얹기다. 1단계는 셋 다와 무관하게 그대로 유효하다.

### 18.1 상황 서술 내보내기 — 특정 모드에 붙이지 않는다

§7.2 가 **들여오기**(바닐라의 말 → 우리 창)라면 이쪽은 **내보내기**(우리 사건 → 바닐라 기록)다.

우리가 쓰는 것은 대사가 아니라 **상황 서술**이다. 바닐라 기록이 하는 그 말투 — "누가
무엇을 했다". 그러면 대사를 짓는 일은 읽는 쪽으로 넘어가고, 우리는 아홉 게임 × 두
언어의 대본을 쓰지 않아도 된다.

**특정 모드에 연결하지 않는다.** 바닐라 자리에 바닐라 방식으로 남겨 두면 그만이고,
읽는 쪽은 읽는 쪽 사정이다. 아래 확인은 "붙일 수 있나"가 아니라 **"그냥 남겨 두는 것만으로
충분한가"** 를 본 것이다.

#### 명제 정정 — 게임 상태를 모르는 것은 **들어오는 쪽**뿐이다

D6 을 "대화는 게임 상태를 모른다"로 읽으면 이 절과 부딪힌다. 정확히는 방향마다 다르다.

| 방향 | 게임 상태를 아나 | 근거 |
|---|---|---|
| 들여오기 (§7.2) | **모른다** | 바닐라가 굴리는 잡담이다. 우리는 창에 얹기만 한다. 그래서 대본이 필요 없다 |
| 내보내기 (18.1) | **안다. 그게 요점이다** | 바깥이 판이 있었다는 걸 아는 통로가 이것뿐이다 |

**제약 4 는 여전히 지켜진다.** 제약 4 는 *코드* 의존이지 정보 차단이 아니다. 내보낼 문장에
들어가는 게임 이름은 `MiniGameDef.label` 이라는 **데이터**에서 오고, `Framework/` 는 여전히
어떤 게임의 코드도 모른다. `playThought` · `vanillaJob` 이 이미 그렇게 하고 있다.

| 내보낼 것 (프레임워크가 안다) | 내보내지 않을 것 |
|---|---|
| 판이 시작됐다 / 끝났다, 누가 이겼다 | 체스의 수, 포커의 패 같은 게임별 세부 |
| 기권했다 · 중단했다(배고픔 등) | `MiniGameWorker.Log` 의 UI 용 짧은 줄 |
| 상대가 사라져 판이 끝났다 | |
| 게임 이름, 난이도, 몇 판째인지 | |

#### 확인한 것 (림톡 `cj.rimtalk` 3551203752, 1.6 어셈블리)

| 확인 | 결과 |
|---|---|
| 남의 상호작용을 읽나 | **읽는다.** 설정 "Override All Interactions with AI Dialogue" 의 툴팁이 *"including vanilla RimWorld and other mods"*. 어셈블리에 `ProcessNonRimTalkInteractions` |
| 기록을 읽나 | **읽는다.** `get_PlayLog` |
| 폰이 뭘 하는지 어떻게 아나 | 프롬프트에 `Activity:` 칸이 있고 `GetReport` · `get_CurJob` · `get_CurJobDef` 를 쓴다 |
| 모임도 보나 | 프롬프트 문자열에 `LordJob_` 이 있다 |

**결론: 우리 쪽에 림톡을 아는 코드는 필요 없다.** 바닐라 자리에 남겨 두면 읽어 간다.

#### 그래서 실제로 할 일은 두 줄이다

1. **우리 `InteractionDef` 를 만들어 `pawn.interactions.TryInteractWith(상대, def)` 로 쏜다.**
   `logRulesInitiator` 에 서술 문장을 넣으면 바닐라 `PlayLogEntry_Interaction` 이 그것을 싣는다.
   Harmony 0 개(우리 토일에서 부르는 공개 호출), 바닐라 Def 수정 0 개.
   - 어떤 `InteractionDef` 를 쓸지는 `MiniGameDef` 의 새 필드로 둔다. **데이터라서 제약 4 가 안 깨진다.**
   - `socialFightBaseChance` 는 **바닐라 필드다.** 사교적 다툼은 코드가 아니라 숫자 하나다.
     `initiatorThought` · `recipientThought` · `initiatorXpGainSkill` 도 마찬가지로 공짜다.
   - **실패 조건이 쿨다운만이 아니다.** `CanInteractNowWith` 가 보는 것: 120틱 쿨다운, 상대가
     `Spawned`, **6칸 이내 + 시야**(`IsGoodPositionForInteraction`), 발신·수신 자격(깨어 있음 ·
     말할 수 있음 · 상호작용 차단 아님). 마주 앉은 두 사람은 대개 통과한다 — 바닐라 폰이
     식탁 너머로 늘 떠드는 것과 같은 조건이다. 그래도 **실패는 정상이다.** 판의 마디에서만
     쏘고, 실패하면 그 마디는 조용히 넘긴다 — 재시도로 매달리지 않는다.
2. **우리 JobDriver 의 `GetReport()` 를 덮어쓴다.** 지금은 `heading over to play.` 한 줄이다.
   붙잡는 토일이 생기면 "누구와 무슨 게임을 하는 중"이 되어야 한다. **가상 함수 하나이고
   가장 싼 개선이다.**

> **`LogEntry` 하위 클래스는 만들지 않는다.** 세이브에 클래스 이름이 박혀서 모드를 빼면
> 그 항목을 못 읽는다. 림톡은 실제로 그 길을 갔고(`PlayLogEntry_RimTalkInteraction`,
> `SaveGamePatch`), 번역 파일에 *"Failed to save/load interaction texts. Resetting data to
> prevent save corruption."* 라는 문구까지 있다. 그 대가를 우리는 치르지 않는다.

> **정정 — `InteractionDef` 쪽도 "조용히 null" 이 아니다.**
> `PlayLogEntry_Interaction.ExposeData` 는 `Scribe_Defs.Look(ref intDef, "intDef")` 다.
> 모드를 빼면 참조는 null 이 되지만 **항목은 세이브에 그대로 남고**, `GetSymbol` ·
> `GetTipString` · `ToGameStringFromPOV_Worker` · `ToString` 이 `intDef.logRulesInitiator` ·
> `intDef.LabelCap` · `intDef.label` 을 **null 검사 없이 쓴다.** `PlayLog.ExposeData` 에
> 걸러 내는 코드도 없다. 커스텀 `LogEntry` 보다는 낫지만 **안전하지는 않다.**
>
> 완화 요인이 하나 있다 — `PlayLog` 는 150개만 남기고 계속 밀려나므로 노출은 "빼기
> 직전에 둔 판" 정도로 한정된다. 그래도 **"넣고 빼도 안전하다"는 약속에는 못 미친다.**
> 낼 거면 정리 전략을 같이 정해야 한다: 판이 끝날 때 우리가 남긴 항목을 지우거나,
> 서술을 우리 창에만 남기고 `PlayLog` 를 아예 안 건드리거나.
> **이건 아직 열린 질문이다. 앞서 닫힌 것처럼 쓴 것은 틀렸다.**

**부수 효과 하나.** 내보내기를 `TryInteractWith` 로 하면 그 결과가 바닐라
`PlayLogEntry_Interaction` 이므로, **§7.2 의 읽는 코드가 우리 사건도 그냥 집는다.**
들여오기와 내보내기가 같은 관을 양방향으로 쓴다 — 별도 경로가 없다.

### 18.2 모으기 — 의식이 아니라 **코어의 모임** 뼈대로

의식 뼈대는 쓸 수 없다. `RitualBehaviorDef` 인스턴스가 **코어에 0개**이기 때문이고,
우리는 DLC 없이 도는 모드다.

> **정정.** "이데올로기 전용"이라고 썼던 것은 틀렸다. 실제 분포는 이데올로기 12 ·
> 로열티 2 · 바이오텍 1 · 오디세이 1 이다. 여러 DLC 가 함께 쓰는 프레임워크이고,
> **코어에만 없다.** 결론은 같지만 이유가 다르다.

**코어에 같은 모양이 있다.** `Core/Defs/GatheringDefs/Gatherings.xml` 의 `GatheringDef` —
`Party` 와 `MarriageCeremony` 둘이다. `duty` · `workerClass` · `gatherSpotDefs` 로
사람을 모으고 데려오고 붙잡고, 끝나거나 취소되는 것까지 그 안에 있다.

그중 **결혼식이 우리 모양이다.** 파티는 "여럿이 자유롭게" 지만, 결혼식은
**주인공 둘이 정해진 자리에 서고 나머지가 본다** — 그게 정확히 우리가 그리는 그림이다.
`MarriageSpot` 이 `spectateDistance` · `allowedSpectateSides` 를 쓰는 것도 그래서다.

> **정정 — Def 만 바꿔 재사용할 수 있는 게 아니다.** `GatheringWorker` 는 추상 클래스이고
> `TryFindGatherSpot` · `CreateLordJob` 두 개가 필수 구현이다.
> `GatheringWorker_MarriageCeremony` 는 약혼 관계를 직접 읽어
> `LordJob_Joinable_MarriageCeremony` 를 만들고 `RCellFinder.TryFindMarriageSite` 까지
> 결혼에 묶여 있다. **재는 대상은 "Def 하나"가 아니라 새 `GatheringWorker` +
> `LordJob` + `LordToil` 세 벌이다.**

그래도 잴 값은 있다. 되면 **§6 의 커스텀 `Hold` 토일이 통째로 필요 없어질 수 있다.** 모으기·데려오기·
붙잡기·중단이 이미 그 안에 있고, 파티는 원래 사교 상호작용이 일어나라고 만든 장치라
D6 도 공짜로 더 잘 굴러간다.

**그래서 2단계를 손으로 만들기 전에 이 뼈대부터 재 본다.** 재사용이 되면 2단계가 훨씬
작아지고, 안 되면 §6 대로 간다. 순서를 이렇게 두는 것이 이 항목의 요점이다.

### 18.3 관전 — 붙잡지 않는다, 여가다

**관전은 의무가 아니다.** 부르지도, 모으지도, 붙잡지도 않는다. 여가가 필요한 폰이
지나가다 구경하고, 그 구경으로 여가를 채우고, 다 차면 간다. 그게 전부다.

이 문장 하나로 18.2 와 갈라진다. 18.2 는 **둘 사람**을 모으는 이야기지만 관전은 모으기가
아니다. 뼈대도 다르고, 순서도 서로 묶이지 않는다.

**이 모양은 코어에 이미 있다 — 텔레비전이다.**

| 조각 | 코어 위치 |
| --- | --- |
| `JoyGiver_WatchBuilding` | `Core/Defs/Joy/JoyGivers.xml` (호스슈·후프스톤이 쓴다) |
| `JobDriver_WatchBuilding` | `Core/Defs/JobDefs/Jobs_Joy.xml` |
| 설 자리 계산 | `WatchBuildingUtility.CalculateWatchCells` · `TryFindBestWatchCell` |
| 인원 상한 | `JobDef.joyMaxParticipants` — 바닐라 필드지만 **우리는 못 쓴다**(아래 예약 항목) |
| 지속·기술·이펙터 | `joyDuration` · `joySkill` · `joyXpPerTick` · `building.effectWatching` |

텔레비전이 하는 일이 정확히 이것이다. 여가 욕구가 온 폰이 스스로 걸어와, 정해진 거리
안에 서서, 시간만큼 여가를 얻고, 욕구가 차거나 더 급한 일이 생기면 떠난다. 새로 설계할
것이 거의 없다.

- **§17 의 "콜로니가 선다" 가 여기서는 거의 사라진다.** 관전하는 폰은 **어차피 여가를 쓰고
  있었을 폰**이다. 체스판 뒤에 서 있든 텔레비전 앞에 앉아 있든 노동 시간이 줄지 않는다.
  앞서 "관전자는 그 위험을 키우므로 인원 상한이 필수"라고 적었던 것은 관전자를 불러
  모은다고 가정했을 때의 이야기다. 자발적 여가로 두면 상한은 **성능·연출용 위생**이지
  안전장치가 아니다.
- **리얼리티 모드와 독립이다.** 관전은 폰을 붙잡지 않으므로 §6 의 `Hold` 도, §7 의 결합도
  필요 없다. 리얼리티 모드가 꺼져 있어도 성립한다. 다만 아래의 진행 중 레지스트리가
  선행 조건으로 붙었으므로, **"가장 싸다"고는 더 이상 말할 수 없다**(§19).
- **관전자는 가구를 예약하면 안 된다.** 여기가 이 절에서 제일 위험한 자리다.
  `JobDriver_WatchBuilding` 은 `pawn.Reserve(targetA, job, job.def.joyMaxParticipants, …)` 로
  **가구 자체를 예약한다.** 그런데 `ReservationManager` 는 같은 대상·같은 레이어에 이미
  걸린 예약의 `MaxPawns` 가 지금 요청과 다르면 **개수와 무관하게 즉시 거절**하고, 같아도
  이미 찼으면 거절한다. §6.1 의 참가자 둘이 `maxPawns: 2` 로 가구를 잡고 있으므로
  **바닐라 관전 Job 을 그대로 쓰면 관전자는 0명이 된다.**
  - 어차피 우리 JobDriver 를 쓸 참이므로 답은 간단하다. **관전자는 자기가 설 칸만
    예약하고 가구는 예약하지 않는다.** 대신 `joyMaxParticipants` 가 주던 공짜 상한을
    잃으므로, 상한은 우리가 직접 센다. 위생용이니 그걸로 충분하다.
  - 커스텀 `ReservationLayerDef` 로 층을 나누는 길도 있다. 우리 Def 라 제약 2 는 깨끗하다.
    다만 안 잡아도 되는 것을 잡는 셈이라, 먼저 시도할 쪽은 아니다.
- **설 자리는 우리가 계산한다.** `watchBuilding*` 값은 ThingDef 의 `<building>` 안에 있어서
  `TryFindBestWatchCell` 은 실제 가구 Def 를 읽는다. 남의 가구에 태그를 얹지 않는다 —
  가구 둘레 사각형을 직접 구하면 되는 단순 기하이고, 그래야 제약 2 가 깨끗하다.
  (`CalculateWatchCells` 는 `Thing` 이 아니라 `ThingDef` 를 받으므로 기하값만 담은 우리
  ThingDef 를 넘겨 재사용하는 우회도 있다. 쓰지도 않을 ThingDef 를 게임에 하나 더
  등록하는 값이라, 직접 계산과 견줘 보고 정한다.)
- **볼 것이 있어야 한다 — 그런데 "지금 두는 중"을 물을 데가 없다.** 판이 돌고 있지
  않으면 관전이 성립하지 않는데, 저장된 `GameSession` 의 존재로는 판단할 수 없다.
  `saveSessions` 를 끄면 세션이 아예 안 남고, 진행 중 표시는 `GameComponent_Recreation.ActiveSession`
  **하나뿐**이라 창이 둘 열리면 마지막 것만 남는다. **가구별 "지금 두는 중" 레지스트리가
  먼저 필요하다** — 저장용 세션과는 별개다. 이건 관전보다 앞선 선행 작업이다. §7.2 의 창 안의 말과 18.1 의
  나레이션이 여기서 값을 한 번 더 한다 — 구경거리의 내용이 그쪽에서 나온다.
- **여가 종류**는 게임을 따라간다. 체스를 구경하면 `Gaming_Cerebral`, 다트를 구경하면
  `Gaming_Dexterity` 쪽이 자연스럽다. 이미 `MiniGameDef` 가 아는 값이다.

**내기는 그 위에 따로 얹는다.** 관전이 자발적이므로 **내기 때문에 서 있는 폰은 없다** —
구경하러 온 김에 거는 것이지 걸러 오는 것이 아니다. 배관은 이미 있다: 슬롯 지갑
(`GameComponent_Recreation` 의 범용 카운터)과 `ColonySilver`(정착지가 실제로 닿을 수 있는
은만 세는 것, 슬롯 충전이 쓴다).

- **돈벌이가 되면 안 된다.** 폰끼리 주고받는 제로섬으로 두어 식민지 총액이 변하지 않게
  하거나, 아주 작은 판돈으로 묶는다. 슬롯에서 은을 도로 내주는 길을 일부러 막은 것과
  같은 이유다 — 오락이 환전소가 되는 순간 게임이 아니라 경제가 된다.
- 이 방향은 이미 있는 룰렛·슬롯과 한 식구다. 카지노 정체성이 자연스럽게 이어진다.

---

## 19. 교차검증 대장 (2026-09-09)

Codex 교차검증 17건. **전부 실물로 다시 확인했다** — 디컴파일(`ilspycmd`)한
`Assembly-CSharp.dll`, 코어 Def 파일, 우리 소스. 확인되지 않은 지적은 받지 않았다.

### 뒤집힌 것 — 명세가 틀렸던 자리

| 어디 | 틀렸던 말 | 실제 |
|---|---|---|
| 18.3 | 관전 인원 상한이 `joyMaxParticipants` 로 공짜다 | `JobDriver_WatchBuilding` 이 **가구를 예약한다.** 참가자 둘이 `maxPawns: 2` 로 잡고 있으면 **관전자는 0명이다** |
| 18.1 | 모드를 빼면 `InteractionDef` 참조가 조용히 null 이 된다 | 참조는 null 이 되지만 **항목이 남고**, 표시 코드가 null 검사 없이 쓴다 |
| 18.2 | `GatheringDef` 는 Def 만 바꿔 재사용할 수 있다 | `GatheringWorker` 는 추상이고 결혼식 구현은 약혼에 묶여 있다. **세 벌을 새로 써야 한다** |
| 18.2 | `RitualBehaviorDef` 는 이데올로기 전용이다 | 이데올로기 12 · 로열티 2 · 바이오텍 1 · 오디세이 1. **코어에만 0개다** (결론은 같다) |
| D6 | 붙여 두면 바닐라가 대화를 굴린다 | 후보를 **같은 팩션에서만** 뽑는다. **방문객에게는 해당 없다** |
| D4 | 자리를 뜨면 판이 가구에 남는다 | `saveSessions` 를 끄면 지워진다. **설정 조건부였다** |
| 곁가지 | 그래브십은 `invalidateOnMove` 때문에 무효화된다 | 이륙 시 `DeSpawn(WillReplace)` → **설정 게이트보다 먼저 `BoardAlive` 에서 걸린다** |

### 빠져 있던 것 — 명세가 말하지 않던 자리

| 어디 | 무엇 |
|---|---|
| §4 | 기즈모(`pawn=null`)와 저장된 판 재개가 **진입 흐름을 통째로 우회한다** |
| §6.1 | 가구만 예약해서는 **같은 폰을 두 판의 상대로 지목**하는 것을 못 막는다 |
| §8 | `seatedPawn` 쪽 소멸은 여전히 검사하지 않는다 — 축은 "상대"가 아니라 "참가자"다 |
| §9 | 여가·숙련 스캐너가 `vanillaJob` 으로만 표를 만든다. **참가자는 아무것도 못 받는다** |
| 18.3 | "지금 두는 중"을 물을 데가 없다. `ActiveSession` 은 **하나뿐**이다 |
| §7.2 | `PlayLog` 폴링에 커서가 없다 — 창을 열면 옛 대화가 쏟아진다 |
| 18.1 | `TryInteractWith` 실패 조건이 쿨다운만이 아니다 (6칸 · 시야 · 각성 · 발화) |

### 살아남은 것

명세의 나머지 주장은 확인을 통과했다. `TryInteractWith` 는 공개 호출이고
(`bool TryInteractWith(Pawn, InteractionDef)`), `socialFightBaseChance` 를 비롯한
`InteractionDef` 필드는 전부 바닐라이며, `JoyGiver_WatchBuilding` · `JobDriver_WatchBuilding` ·
`WatchBuildingUtility` · `GatheringDef` 는 코어다. 림톡이 남의 상호작용을 읽는다는 것도
그대로다. **Harmony 없이 안 되는 항목은 하나도 나오지 않았다.**

### 순서에 미친 영향

1단계(상대 고르기)는 그대로 첫 조각이다. 다만 **§4 의 우회 경로와 §6.1 의 상대 예약이
1단계 안으로 들어온다** — 나중에 붙일 수 있는 것이 아니라, 이것 없이는 1단계가
"켰는데 어떤 길로는 그냥 열린다"가 되기 때문이다.

18.3(관전)은 "셋 중 가장 먼저 낼 수 있다"고 적었지만, **가구별 진행 중 레지스트리가
선행 조건으로 붙었다.** 그 값을 치르기 전에는 가장 싸지 않다.

---

## 곁가지 — 리얼리티 모드와 무관하게 확인할 것

**원인을 잘못 짚었다.** `invalidateOnMove` 가 아니다.

`Invalidation.Check` 는 맨 앞에서 `BoardAlive` 를 본다 — `board.Spawned && !board.Destroyed`.
그런데 그래브십은 이륙할 때 실려 가는 물건을 먼저 `DeSpawn(DestroyMode.WillReplace)`
한다. 그러면 `Spawned` 가 꺼지고, **설정 게이트에 닿기도 전에** `Destroyed` 로 무효화된다.
`invalidateSessions` 를 꺼 둬도 막히지 않는다 — 그 검사는 설정 위에 있다.

착륙 좌표도 `root + local` 이라 "좌표가 통째로 바뀐다"는 것도 늘 맞지는 않는다.
그러니까 이건 이동 감지 문제가 아니라 **"잠깐 사라졌다 돌아오는 것"을 파괴와 구별하지
못하는 문제다.** 지원하려면 `WillReplace` 상태를 따로 알아봐야 한다. 지금도 걸린다.
