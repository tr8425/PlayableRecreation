# 몰입 모드 2단계 — 명세

> 상태: **네 단계 전부 구현됨** (2026-09-09, 미출시). 상대를 골라 둘이 판 앞에 서고,
> 판이 끝날 때까지 거기 서서, 사라진 사람과 손님의 우호도까지 이야기가 닫힌다.
> **인게임 확인은 아직 안 했다** — §16 이 그 목록이다. 어디까지 됐는지는 §15 표를 본다.
> 이름: 작업 중에는 "리얼리티 모드"라 불렀다. **독립 기능이 아니라 이미 있는
> 몰입 모드의 두 번째 칸으로 넣는다**(§0). 파일 이름은 링크가 걸려 있어 그대로 둔다.
> 계기: ferny(창작마당, 2026-09-09) — 검토 기록은 `Feedback/FEEDBACK.md` 의 같은 날 항목.
> 제약은 `HANDOFF.md` §2 를 따른다. 이 문서는 그것을 다시 쓰지 않고 §14 에서 점검만 한다.
> 읽기 좋은 판: <https://claude.ai/code/artifact/2f4e50e1-6be6-4db6-acaa-ffb569bbad49>
> (내용은 같다. **정본은 이 파일이다** — 고칠 때는 여기를 먼저 고친다.)

---

## 0. 한 줄

**상대를 식민지 사람 중에서 고르고, 둘을 실제로 판 앞에 세운다.**

AI 는 손대지 않는다. 이 기능이 하는 일은 기존 AI 에게 **이름과 몸을 주는 것**이다.
이 한 문장이 이 기능이 감당 가능한 크기인 이유다.

### 이름을 새로 짓지 않는다 — 몰입 모드의 두 번째 칸이다

새 이름을 고민하다 **이름이 필요 없다는 결론이 났다.** 이미 있는 몰입 모드
(`PR.Settings.Immersion` = "몰입 모드 (정착민이 가구까지 걸어간 뒤 시작)")가 하는 일이
지금은 *걸어가는 것 하나*뿐이고, 이 기능은 정확히 그 다음 칸이기 때문이다.

| | 하는 일 |
|---|---|
| 몰입 모드 **끔** | 지금처럼 즉시 창이 열린다 |
| 몰입 모드 **1칸** (지금의 몰입 모드) | 고른 폰이 가구까지 걸어간 뒤 창이 열린다 |
| 몰입 모드 **2칸** (이 문서) | 상대를 고르고, 둘이 실제로 앉고, 판이 끝날 때까지 거기 있다 |

**하나의 축이다.** "판이 세계 안에서 얼마나 실제로 일어나는가" — 그 축을 한 칸 더
미는 것이지 다른 기능이 아니다. 그래서 설정도 하나로 둔다.

**부모 체크박스는 건드리지 않는다.** `immersionMode` 는 이미 나간 `bool` 이고 세이브에
`"immersionMode"` 로 박혀 있다. 값의 뜻을 바꾸면 이미 켜 둔 사람의 게임이 조용히
달라진다. **2칸은 그 아래 하위 항목으로 붙인다** — 부모가 꺼져 있으면 회색이고, 이유를
한 줄로 말한다.

- **D1(일시정지 배타)과 `saveSessions` 요구는 하위 항목에만 붙는다.** 지금 몰입 모드를
  켜고 일시정지도 켜 둔 사람이 갑자기 일시정지를 잃으면 안 된다.
- **「대전」·「대결」 계열은 어차피 피했을 이름이다** — 창작마당에서 이미 두 사람이
  멀티플레이로 오해했다. 하위 항목 이름은 그래서 기능 설명으로 둔다:
  *"상대를 골라 둘이 함께 둔다"*.
- **세부도 전부 그 아래로 간다.** 범위·성격·우호도처럼 취향이 갈리는 값은 코드가 정하지
  않고 설정으로 올린다(§12). 2칸을 안 켠 사람의 설정 창은 지금과 한 줄도 다르지 않다.

## 1. 왜

> "I really wish you actually had to select one of the other colonists to be your
> opponent. It feels currently like it sorta reverses the immersion it brings to
> the table by having you play against invisible AIs and not actual people in
> your colony" — ferny

맞는 지적이다. 이 모드는 "식민자가 하던 것을 직접 한다"로 몰입을 만들어 놓고,
막상 앉으면 맞은편이 `Opponent (Good)` 라는 난이도 이름뿐이다. 판은 진짜 물건인데
상대만 진짜가 아니다.

## 2. 하지 않는 것

명세는 안 하는 것을 적어야 커지지 않는다.

- **멀티플레이가 아니다.** 사람 대 사람은 이 문서의 범위 밖이다.
- **AI 를 새로 만들지 않는다.** 상대 폰은 기존 난이도 AI 의 얼굴이다.
  체스 AI 가 그 폰의 성격·특성을 반영하지 않는다.
- **대화를 만들지 않는다. 다만 보여는 준다.** 무슨 말을 할지는 바닐라가 정한다.
  우리는 그것을 창 안으로 끌어올 뿐이다(§7.2). 게임을 아는 대사를 쓰는 것은 다른 기능이다.
- **일시정지와 같이 켜지 않는다.** → D1.
- **판 위의 말을 폰이 실제로 옮기지 않는다.** 애니메이션·이펙트는 범위 밖이다.

## 3. 결정 (다시 뒤집지 않기로 한 것)

### D1. 2칸과 `pauseWhilePlaying` 은 함께 켜지지 않는다

멈춰 두면 2칸이 하려던 것이 전부 같이 멈춘다. 습격이 올 수 없으니
"체스를 두다 전투에 나간다"가 사라지고, 시간이 안 흐르니 두 폰이 대화할 일도
사교 다툼이 날 일도 없다. **2칸은 시간이 흐르는 것이 요점이다.**

하위 항목을 켜면 `pauseWhilePlaying` 은 꺼지고, 왜 그런지 한 줄로 말한다.
둘 다 켜진 세이브를 읽을 때는 2칸이 이긴다.

**부모(몰입 모드 1칸)에는 이 배타가 걸리지 않는다.** 걸어가서 여는 것과 멈추는 것은
서로 방해하지 않고, 이미 그렇게 쓰고 있는 사람의 설정을 뒤집을 이유가 없다.

### D2. 난이도는 정해 주는 값이 아니라 **제안값**이다

상대를 고르면 그 사람의 스킬로 난이도를 미리 짚어 준다. 그러나 플레이어가 바꿀 수 있다.

정해 주는 쪽으로 가면 전적 장부가 식민지 구성에 잠긴다 — `GameRecord` 도 숙련도도
난이도 칸으로 쌓이는데, 걸작 칸을 열려면 걸작급 식민자가 있어야 한다. 뉴럴 트레이너
한 방이나 적성 유전자 하나로 칸이 통째로 바뀌기도 한다. ferny 가 지적한 것은
**상대에게 얼굴이 없다는 것**이지 난이도를 뺏어 달라는 것이 아니다.

### D3. 판의 난이도는 시작할 때 얼어붙는다

지금 이미 그렇게 되어 있다 — `GameSession.tier` 가 세이브에 박히고, 이어 하기 생성자는
`game.ClampTier(resumed.tier)` 로 그 값을 **다시 읽는다.** 2칸이 이걸
"상대의 지금 실력으로 다시 계산"으로 고치면 안 된다. 두던 판이 도중에 어려워지는 것은
"창을 다시 열어도 결과가 다시 굴러가지 않는다"는 약속을 깨는 일이다.

`ClampTier` 를 거치는 것도 그대로 둔다. 난이도 단계 수를 나중에 줄여도 옛 판이
터지지 않는 이유가 그 한 줄이다.

### D4. 배고픔·피로는 멈춤이 아니라 **자리를 뜨는 것**으로 푼다

붙잡는 토일에 탈출구를 단다. 배가 고프면 일어나 밥을 먹으러 가고, 두던 판은
`saveSessions` 가 이미 가구에 남겨 준다. 돌아와서 이어 둔다.

> **정정.** `saveSessions` 는 켤 수 있는 설정이고 끄면 `Dialog_MiniGame.SaveSession` 이
> 창을 닫을 때 세션을 지운다. 그러면 "밥 먹고 와서 이어 둔다"가 성립하지 않는다.
> **2칸에서는 세션 저장이 선택이 아니다** — 켜져 있어야 하고, 꺼져 있으면
> 하위 항목을 켤 수 없다. §12 에서 정한다.

우리가 가진 기능이 그대로 답이 되고, 이야기로도 이쪽이 낫다 — 저녁 먹으러 판을
비우는 두 사람.

### D5. 상대가 사라진 판은 되살리지 않는다

죽었든 납치됐든 캐러밴을 탔든, 그 판은 끝난 판이다. 상대를 갈아 끼워 이어 하는 길은
만들지 않는다. 반쯤 둔 판은 그 상대와의 그 판이다.

### D6. 대화는 바닐라가 굴리고, **창이 그것을 보여 준다**

`Pawn_InteractionsTracker` 가 가까이 있는 폰끼리 알아서 굴린다는 가정 위에 선다.
**이제 가정이 아니다** — 2026-09-10 에 디컴파일로 사슬을 끝까지 확인했다(§16-1).
사교 모드도, 주기도, 자리도, 남는 곳도 우리가 기대한 그대로다.

> **범위 정정 — 방문객에게는 해당하지 않는다.** `TryInteractRandomly` 는 후보를
> `pawn.Map.mapPawns.SpawnedPawnsInFaction(pawn.Faction)` 에서만 뽑는다. 다른 팩션의
> 방문객은 판을 사이에 두고 마주 앉아도 **서로의 자연 대화 후보가 아니다.**
> 그래서 §11 의 방문객 대전에서 D6 은 굴러가지 않는다 — 그쪽은 18.1 의 명시적
> `TryInteractWith` 가 유일한 통로다. 자연 대화는 **같은 팩션 참가자에게만** 기대한다.

**다만 굴러도 플레이어는 못 본다.** 말풍선은 지도 위에 뜨는데 플레이어는 판을 보고
있다. 폰 둘을 붙잡아 두는 값을 치르고 아무것도 안 보이면 2단계는 뜻이 없다.
그래서 바닐라가 만든 말을 **창 안으로 끌어온다**(§7.2). 만드는 것이 아니라 옮기는 것이다.

게임을 아는 **대사**("그 수는 좀…")는 여기 포함되지 않는다 — 아홉 게임 × 두 언어의
글쓰기 과제다. 다만 **"게임 상태를 모른다"는 것은 이 들여오는 방향에만 해당한다.**
반대로 내보내는 상황 서술은 게임을 알아야 뜻이 있고, 그건 대사가 아니라 데이터라서
제약 4 와 부딪히지 않는다 — 18.1 에서 정리했다.

---

## 4. 진입 흐름

```
식민자 선택 → 가구 우클릭
  │
  ├─ 2칸 꺼짐 → (지금과 같다 — 1칸이면 걸어가고, 끔이면 즉시)
  │
  └─ 2칸 켜짐 · 승부 게임
        │
        1. 상대 고르기 (FloatMenu — 자격 있는 식민자·방문객, §10)
        │     자격 없는 사람은 이유와 함께 회색으로 (바쁨 / 못 감 / 능력)
        2. 난이도 창 — 고른 상대의 실력으로 짚어 준 칸이 미리 선택됨 (D2)
        3. 두 사람에게 Job 을 준다
        │     주도한 쪽: PR_GoToGame  (걸어감 → 창 열기 → 붙잡기)
        │     상대  쪽: PR_JoinGame   (걸어감 → 붙잡기)
        4. 둘 다 도착하면 창이 열린다
        │     한쪽이 못 오면 판을 열지 않고 한 줄로 알린다
        5. 판이 끝나거나 창이 닫히면 두 Job 이 함께 끝난다
```

**진입점은 이 하나가 아니다.** 지금 판을 여는 길이 세 갈래고, 나머지 둘은 이 흐름을
통째로 건너뛴다.

| 진입점 | 지금 하는 일 | 2칸에서 |
|---|---|---|
| 폰 우클릭 → FloatMenu | `GameEntry.Begin(game, board, selPawn)` | 위 흐름 그대로 |
| **가구만 선택 → 기즈모** | `GameEntry.Begin(game, parent, null)` — **폰이 없다** | 폰 없이 앉힐 수 없다. 자리를 채울 사람을 고르게 하거나 기즈모를 접는다 |
| **저장된 판 이어 두기** | `GameEntry.Resume(session)` 직접 호출 | 걷기·붙잡기를 건너뛴다. **재개도 같은 준비 절차를 타야 한다** |

**이건 새로 생기는 문제가 아니라 몰입 모드가 이미 가진 구멍이다.** `GameEntry.Begin` 은
`pawn != null && PRMod.Settings.immersionMode` 일 때만 걸어가게 한다. 기즈모는
`pawn == null` 로 부르므로 **지금도** 몰입 모드를 켜 둔 채 가구를 클릭하면 아무도 걷지
않고 창이 열린다. 저장된 판을 이어 두는 길(`Resume`)도 마찬가지다.

그러니 이 수선은 2칸이 만들어 낸 새 비용이 아니라 **1칸에 원래 있었어야 할 일**이다.
세 갈래를 한 문으로 모으면 몰입 모드 자체가 먼저 좋아진다.

**4번의 "둘 다 도착"이 이 흐름의 유일한 새 동기화 지점이다.** 주도한 쪽이 먼저 닿으면
상대를 기다린다. 기다리는 동안 창은 열지 않는다 — 빈 의자를 보여 줄 이유가 없다.

## 5. 데이터 모델

### 5.1 `GameSession`

```csharp
public Pawn opponentPawn;          // 2칸이 아니면 null
Scribe_References.Look(ref opponentPawn, "opponentPawn");
```

`GameSession` 은 `*SaveData` 가 아니므로 클래스 이름 제약(제약 7)에 걸리지 않는다.
필드 하나를 더하는 것은 세이브 호환을 깨지 않는다 — 옛 세이브는 null 로 읽힌다.

`seatedPawn` 의 뜻은 바뀌지 않는다: **플레이어가 조작하는 쪽**이다.

### 5.2 새 Def

| 종류 | defName | 쓰임 |
|---|---|---|
| `JobDef` | `PR_JoinGame` | 상대를 판 앞으로 부르고 붙잡아 둔다 |
| `ThoughtDef` | `PR_OpponentGone` | 상대가 사라진 판 앞에 다시 선 사람의 기분 |
| `ThoughtDef` | `PR_PlayedTogether` | (선택) 둘이 한 판을 끝냈을 때 |

전부 우리 Def 다. 바닐라 Def 는 건드리지 않는다(제약 2).

### 5.3 새 `InvalidationReason`

`OpponentGone` 하나만 더한다. 죽음·납치·추방·캐러밴·맵 이탈·적대화를 값 여럿으로
쪼개지 않는다 — 번역 키만 늘고 플레이어가 얻는 것은 같다. 문구에서 누구였는지만 말한다.

**다운은 여기 넣지 않는다.** 다운된 폰은 `Spawned` 이고 `Dead` 가 아니며 같은 맵에 있다 —
§8 의 판정식에 걸리지 않는다. 넣을지 말지를 정해야 하는데, **넣지 않는 쪽으로 정한다.**
다운은 사라진 것이 아니라 자리를 뜬 것이고(D4), 일어나면 이어 둘 수 있어야 한다.
대신 Job 은 끊긴다 — 판은 가구 위에 남고 상대는 병상에 있다.

## 6. Job — 붙잡는 토일

### 6.1 예약

지금 `TryMakePreToilReservations` 는 아무것도 예약하지 않고 `true` 를 돌려준다.
Job 이 즉시 끝나므로 지금은 무해하지만, 붙잡기 시작하면 같은 가구를 두 판이 물거나
상대로 지목된 폰이 다른 판에도 불려 간다.

```csharp
return pawn.Reserve(job.targetA, job, maxPawns: 2, stackCount: 0, errorOnFailed: errorOnFailed)
    && pawn.Reserve(opponent, job, maxPawns: 1, stackCount: 0, errorOnFailed: errorOnFailed);
```

`maxPawns: 2` 는 바닐라가 2인용 가구에 쓰는 관용이다. 예약은 다른 모드와 부딪히기 쉬운
자리이므로 실패를 조용히 받아 "지금은 쓸 수 없다"로 끝낸다.

**가구만 예약해서는 상대 중복을 못 막는다.** 보드가 둘이면 각각 두 자리를 따로 잡으므로,
같은 폰을 두 판의 상대로 지목하는 것이 예약으로 걸리지 않는다. 두 번째 지목이 첫 판의
Job 을 끊는 모양으로 나타난다. **상대 폰 자체를 예약한다.** 그리고 두 Job 중 하나라도
시작에 실패하면 **양쪽을 함께 되돌린다** — 한 사람만 걸어가다 서 있는 그림을 만들지 않는다.

> **`ReservationManager` 의 규칙.** 같은 대상·같은 레이어에 이미 예약이 있고 그
> `MaxPawns` 가 지금 요청과 다르면 **개수와 상관없이 즉시 거절된다.** 같아도 이미
> `maxPawns` 만큼 차 있으면 거절이다. 이 규칙이 18.3 의 관전과 정면으로 부딪힌다 —
> 거기서 다시 다룬다.

### 6.2 토일 구성

```
Toils_Goto.GotoThing(TargetIndex.A, PathEndMode.Touch)
FaceTarget
[주도한 쪽만] 상대가 닿을 때까지 대기 → 창 열기
Hold  (ToilCompleteMode.Never)
```

### 6.3 `Hold` 의 종료 조건

| 조건 | 결과 | 비고 |
|---|---|---|
| 판이 끝났다 / 창이 닫혔다 | `Succeeded` | 정상 |
| 배고픔이 임계 아래 | `InterruptForced` | **D4. 이게 굶주림의 답이다** |
| 피로가 임계 아래 | `InterruptForced` | 〃 |
| 상대의 Job 이 끝났다 | `Succeeded` | 혼자 남지 않는다 |
| 가구가 사라짐·금지됨 | `Incompletable` | `FailOnDespawnedNullOrForbidden` |
| 징집·정신 붕괴·다운 | (바닐라가 처리) | Job 이 알아서 끊긴다 |

임계값은 설정으로 빼지 않는다. 바닐라 여가 Job 이 쓰는 값에 맞춘다.

여가(joy)는 붙잡혀 있는 동안 쌓되, **여가가 가득 찼다고 판을 끊지 않는다.**
바닐라 여가 Job 은 가득 차면 끝나지만 여기서는 두던 판이 우선이다.

## 7. 창이 폰을 비추는 방식

### 7.1 결합 — 폰이 자리를 뜨면 창도 닫힌다

지금은 둘이 무관하다 — Job 이 즉시 끝나므로 창만 살아 있다. 붙잡기 시작하면
"폰은 일어섰는데 창은 열려 있다"를 정의해야 한다.

**어느 쪽이든 자리를 뜨면 창이 스스로 닫히고 판은 가구에 남는다.** 위협 인터럽트가
이미 쓰는 문법 그대로다. 왜 닫혔는지 한 줄로 말한다("X 가 밥을 먹으러 일어났다").

이때 기권으로 치지 않는다 — `RecordResult` 는 호출하지 않는다. 중단이지 패배가 아니다.

### 7.2 대화를 창 안으로

바닐라 사교 상호작용은 `Find.PlayLog` 에 `PlayLogEntry_Interaction` 으로 쌓인다.
두 폰에 걸린 것만 골라(`LogEntry.Concerns`) 창의 기록 패널에 올린다. 문구는
`ToGameStringFromPOV(seatedPawn)` 이 만들어 준다. Harmony 불필요.

**자리는 이미 있다.** 기록 패널은 프레임워크가 그리고(`Dialog_MiniGame.DrawLog`)
게임이 `MiniGameWorker.Log` 로 줄을 댄다. 사교 줄은 게임이 아니라 **프레임워크가 따로
들고 같은 패널 안에 구분된 띠로 그린다** — 그래야 게임 코드가 한 줄도 안 바뀐다(제약 4).

시간순으로 섞지 않는다. `Log` 는 그냥 문자열 목록이라 시각이 없고, 아홉 게임의 `Log` 를
타임스탬프 있는 구조로 바꾸는 값에 비해 얻는 것이 적다.

**패널이 없는 게임은 애초에 둘이 할 수 없는 게임이다.** `showLog` 가 꺼진 셋은
별 보기·슬롯·펀칭백인데, 앞의 둘은 승부가 없고 펀칭백은 1×1 가구다(§16-4).
그래서 이 설계에 빈칸이 남지 않는다.

폴링은 위협 감시와 같은 잣대로 둔다(0.5초). 매 프레임 `PlayLog` 를 훑을 이유가 없다.

**커서가 필요하다.** `PlayLog` 는 새 항목을 앞(0번)에 꽂고 150개만 남긴다. 커서 없이
훑으면 창을 열 때 옛 대화가 쏟아지고, 0.5초 사이에 여러 항목이 생기면 순서가 엉킨다.

- 창을 연 시점의 마지막 항목을 세션별로 기억하고, **그 뒤에 생긴 것만** 소비한다.
- **양쪽 참가자를 모두 `Concerns` 로 갖는 항목만** 집는다. 옆을 지나가는 사람의
  잡담까지 판 안으로 들어오면 안 된다.
- 150개를 넘겨 밀려난 항목은 그냥 잃는다. 되살리지 않는다.

## 8. 무효화 — 폰이라는 새 축

`Invalidation.Check` 는 지금 전부 가구만 본다(파괴·이동·손상·수리·전투·청소·만료).
`seatedPawn` 은 죽었는지조차 묻지 않는다. 2칸은 여기에 **축을 하나 더한다.**

```
참가자 중 누구든 null · Dead · Destroyed · !Spawned
  · Map != board.Map · HostileTo(player)   →  참가자 소멸
```

**상대만 보지 않는다.** 지금 `Invalidation` 은 `seatedPawn` 이 죽었는지조차 묻지 않는다.
상대 쪽만 새로 검사하면 **플레이어 쪽이 죽은 판을 이어 둘 수 있다** — 그 뒤 머리글·전적·
두 사람 보상이 전부 없는 사람을 다룬다. 축은 "상대"가 아니라 **"참가자"** 로 잡는다.

이유 값은 그래도 하나다(`OpponentGone`). 문구에서 누가 없는지만 말한다.
`invalidateOnOpponentGone` 설정 하나를 붙이되 기본은 켬으로 둔다.

**검사 시점은 바꾸지 않는다.** "두는 중인 판은 검사하지 않는다. 흐트러졌는지는 언제나
돌아왔을 때 알게 된다"는 기존 철학 그대로다 — B 가 판 앞에 다시 섰을 때 비로소
상대가 없다는 것을 안다. 사용자가 그린 장면과 정확히 같다.

그때: 토스트 한 줄 + `PR_OpponentGone` 생각 + 세션 폐기.

## 9. 보상은 누가 받나

| 무엇 | 누구 | 근거 |
|---|---|---|
| 전적 (`GameRecord`) | 판 하나에 한 번 | 난이도는 여전히 플레이어가 고른 칸이다 (D2) |
| 숙련도 (`Mastery`) | 지금과 같이 게임·난이도 단위 | 폰 단위가 아니다 |
| 여가 | **받을 수 있는 쪽만** | 둘 다 앉아 있었지만 `needs.joy` 가 없는 폰이 있다 (§10) |
| 생각 | 둘 다 (`PR_PlayedTogether`) | 선택 항목 |

> **"둘 다"는 지금 저절로 되지 않는다.** 여가·숙련 보너스를 주는
> `MapComponent_Recreation` 의 스캐너는 `MiniGameDef.vanillaJob` 으로만 표를 만든다
> (`ByJob`). `PR_GoToGame` · `PR_JoinGame` 은 그 표에 없으므로 **참가자는 이 경로로
> 아무것도 받지 못한다.** 붙잡는 토일을 만든다고 여가가 저절로 오르지도 않는다 —
> 바닐라 여가 Job 이 아니기 때문이다.
> **두 JobDriver 의 `Hold` 틱에서 여가 획득을 직접 한다.** 방문객에게도 줄지, 숙련도
> 보너스까지 줄지는 §11 · §15 에서 따로 정한다.

**전적에 상대 이름을 남길지는 1단계에서 정하지 않는다.** 남기면 그 사람이 자란 뒤
기록의 뜻이 흐려지는 문제를 같이 풀어야 한다(그때 몇 칸이었는지도 같이 남겨야 한다).
1단계는 `GameRecord` 를 건드리지 않는다.

## 10. 상대 — 자격과 성격

### 10.1 자격

**종족 목록을 쓰지 않는다.** 무엇인지가 아니라 **할 수 있는지**를 묻는다. 목록으로 적으면
새 모드가 나올 때마다 틀리고, 우리가 모르는 폰은 전부 막히기 때문이다.

세 가지만 통과하면 앉는다.

| 질문 | 검사 |
|---|---|
| 1. 우리 Job 을 받아 걸어올 수 있나 | `CanReserveAndReach` · 다운 아님 · 정신 착란 아님 · 거리 상한 안 |
| 2. 앉아서 판을 다룰 수 있나 | 의식 · 조작(말을 옮긴다) · 시각(판을 본다). 게임마다 무엇이 필요한지는 `MiniGameDef` 가 `linkedSkill` 옆에 든다 |
| 3. 아기가 아닌가 | `DevelopmentalStage.Baby` 를 뺀다 |

| 대상 | 되는가 | 왜 그렇게 나오는가 |
|---|---|---|
| 식민자 | ○ | 셋 다 통과 |
| **아기·유아** (0~3세) | **×** | `HumanlikeBaby` = `DevelopmentalStage.Baby`. 3번에서 걸린다 |
| **어린이·프리틴** (3~13세) | **○** | `HumanlikeChild` · `HumanlikePreTeenager` = `Child`. 통과한다 |
| 방문객 | ○ | §11 |
| 죄수·노예 | **범위 밖** | 저항 감소·설득과 얽히면 다른 기능이 된다 |
| 동물 | × | 2번에서 걸린다 (`intelligence` 가 `Humanlike` 미만) |
| 메카노이드 | (검사에 맡긴다) | 바닐라 메카는 `intelligence: ToolUser` 라 2번에서 **저절로** 떨어진다. **명시적으로 막지 않는다** — 여가를 아는 메카를 넣는 모드가 있으면 그 모드가 값을 올렸을 것이고, 그러면 통과하는 게 맞다 |
| 자기 자신 | × | 목록에서 뺀다 |
| 이미 다른 판에 앉은 폰 | × | 예약으로 막힌다 (§6.1) |

**어린이는 대개 최하 난이도가 제안된다.** 능력치가 낮으니 D2 의 제안값이 맨 아래 칸을
짚는다. 문제가 아니다 — 제안일 뿐이고 플레이어가 올릴 수 있으며, 아이가 아이답게
두는 것이 오히려 맞다.

> **주의 — 아이·방문객에게는 여가 욕구가 없다.** 바닐라 `Joy` NeedDef 는
> `colonistsOnly: true` 에 `developmentalStageFilter: Adult` 다(= 13세 이상).
> 그래서 **어린이·방문객·죄수·노예는 `needs.joy` 가 아예 null 이다.**
> 이 사실이 두 곳에 걸린다.
> - **자격 검사에 `needs.joy != null` 을 쓰면 안 된다.** 쓰면 넣으려던 두 부류가
>   그대로 잘려 나간다. 위의 세 질문에 여가 욕구는 들어 있지 않다.
> - **§9 의 "여가는 둘 다"가 그대로는 성립하지 않는다.** 줄 수 있는 쪽에만 간다.

거리: 도달 가능(`CanReach`) 검사는 필수. 거리 상한도 둔다 — 없으면 맵 반대편 사람을
두 시간 기다리는 것이 게임이 된다. **상한값은 설정이다**(`playTogetherRange`, 기본 30칸) —
맵 크기와 정착지 모양에 따라 적정값이 다르다.

**어린이·방문객을 받을지도 설정이다**(`playTogetherChildren` · `playTogetherVisitors`,
둘 다 기본 켬). 위 표는 **켜져 있을 때** 어떻게 되는지를 적은 것이다.

### 10.2 성격이 판에 비치는 방식

**대부분 이미 공짜다.** 두 사람을 붙여 놓기만 하면 바닐라가 성격대로 굴린다. 우리가
쓸 코드는 거의 없고, 새로 만들어야 하는 것은 딱 한 조각이다.

| 성격 | 바닐라가 이미 하는 일 | 우리가 할 일 |
|---|---|---|
| **직설적** (`Abrasive`) | 부정적 상호작용 선택 가중치 **×2.3** (`NegativeInteractionUtility.AbrasiveSelectionChanceFactor`) | **없음.** D6 이 굴리고 §7.2 가 창에 보여 준다 |
| **다정다감** (`Kind`) | 부정적 상호작용 확률이 **0 이다.** 코드가 맨 앞에서 `return 0f` 한다 | 대화 쪽은 없음. **무르기만 우리 몫** |
| **강한 공격성** (`Aggression_HyperAggressive`) | `socialFightChanceFactor: 3` — 사교 다툼 확률 **3배** | 다툼이 나면 판을 어떻게 끝낼지 (§16-5 가 이미 묻고 있다) |
| **사이코패스** (`Psychopath`) | 사교로 기분이 흔들리지 않는다 | **없음.** 아무 반응이 없다는 것 자체가 성격이다 |

**"판 엎기"의 바닐라 이름은 사교 다툼이다.** 18.1 의 `InteractionDef` 에
`socialFightBaseChance` 를 얹어 두면, 강한 공격성 유전자를 가진 폰은 그 값에 자기
3배를 곱해서 실제로 판을 엎는다. 우리가 "엎는다"를 만들 필요가 없다 — **숫자 하나와
바닐라 유전자가 알아서 한다.** 다툼이 나면 Job 이 끊기므로 판도 자연히 끝난다.

**새로 필요한 것은 두 개뿐이다.**

1. **다정다감의 무르기 +1.** 상대가 봐준다는 뜻이다. `Dialog_MiniGame` 이 쓰는
   `PRMod.Settings.undoLimit` 에 이 판만 한 칸을 얹는다.
   - 전적이 흐려지지 않나? **이미 안 흐려진다.** `undoLimit` 은 원래 플레이어가
     설정에서 바꾸는 값이라 원장이 애초에 무르기로 정규화되어 있지 않다. 한 칸을 더
     얹는 것이 새로운 불공정을 만들지 않는다. `flawlessWins`(무르기 0 승)는 그대로 안전하다.
2. **"지고 있나"를 물을 자리.** 사용자의 그림은 *질 것 같으면* 엎는 것인데, 바닐라
   사교 다툼은 결과와 무관하게 무작위다. 판세를 반영하려면 `MiniGameWorker` 에
   가상 속성이 하나 더 필요하다 — **`HasProgress` 와 같은 패턴**으로, 기본값은
   "모른다"로 두고 답할 수 있는 게임만 답한다.
   ```csharp
   /// <summary>지고 있나. 판세를 말할 수 없는 게임은 null 을 준다.</summary>
   public virtual bool? Losing { get { return null; } }
   ```
   18.1 의 `TakeBeat()` 과 **같은 식구다** — 게임이 규칙을 알고 프레임워크는 종류만 안다.
   둘은 같이 만든다.
   답이 `true` 일 때만 다툼 확률이 큰 `InteractionDef` 로 바꿔 쏜다. **제약 4 는 지켜진다** —
   프레임워크는 "지고 있다"만 알지 그게 체스의 무엇인지는 모른다.

**하지 않을 것: 판의 규칙을 성격으로 바꾸는 것.** 무르기 한 칸은 이미 플레이어가 만지는
값이라 예외지만, 그 밖에 성격이 말의 움직임이나 확률을 바꾸기 시작하면 **아홉 게임 ×
성격 수만큼의 규칙이 생기고** 전적이 무슨 뜻인지 알 수 없게 된다. 성격은 **말과 끝내는
방식**에만 비친다.

**전부 끌 수 있다**(`playTogetherPersonality`). 그 아래 두 줄이 따로 있다 —
`playTogetherBoardFlip` 과 `playTogetherKindUndo`(§12). 특히 판 엎기는 **판이 남의 성격
때문에 끝나는 것**이라, 사람에 따라 이야기가 아니라 짜증이다. 끌 수 있어야 한다.

**단계**: 표의 "우리가 할 일 = 없음" 칸은 2단계(붙잡기)가 끝나는 순간 그냥 따라온다.
무르기와 `Losing` 은 4단계 다듬기로 둔다.

## 11. 방문객과 우호도

한 판을 끝까지 두면 그 방문객의 팩션 우호도가 움직인다
(`Faction.TryAffectGoodwillWith`, Harmony 불필요).

### 11.1 결과에 따라 움직인다

고정값 한 번이 아니라 **판이 어떻게 끝났는지**를 본다. 난수도 섞는다 — 계산식처럼
읽히면 이야기가 아니라 표가 된다.

| 판 | 우호도 | 왜 |
|---|---|---|
| 접전 끝에 끝났다 | **+ 크게** | 손님이 즐거웠다. 이게 최고값이다 |
| 평범하게 끝났다 | + 조금 | 시간은 잘 보냈다 |
| 손님이 압도적으로 이겼다 | + 조금 | 이겼으니 나쁘진 않다. 다만 승부가 아니었다 |
| **손님이 압도적으로 졌다** | **−** | 손님을 짓밟았다. 손님을 부른 자리가 아니게 됐다 |
| 우리 쪽이 중간에 기권했다 | − 조금 | 판을 엎고 일어선 것이다 |

**"압도적 패배"의 주어를 뒤집어 둔다.** 플레이어가 크게 지는 게 아니라 **손님이 크게
지는 쪽**이 마이너스다. 이유가 둘이다 — 이야기로는 손님을 뭉개는 게 실례이고,
설계로는 **플레이어가 못해서 벌을 받는 구조가 아니게 된다.** 잘하면 잘할수록 조심해야
하는 자리가 생기는 편이 낫다.

### 11.2 파밍을 막는 두 가지

우호도는 은으로도 못 사는 전략 자원이다. 그냥 두면 **체스판이 동맹 자판기가 된다.**

1. **난이도가 크기를 정한다.** D2 때문에 난이도는 플레이어가 고르는 값이다. 폭만 보고
   값을 주면 맨 아래 칸에서 압승만 반복하는 게 최적 전략이 된다. **낮은 칸에서는
   폭이 커도 거의 안 움직인다.** 위 표의 크기는 전부 난이도로 한 번 곱한다.
2. **방문 1회당 한 번.** 같은 방문객 무리에게 여러 판을 두어도 우호도는 한 번만
   움직인다. 여러 판을 뒀다면 **가장 나중 판**을 쓴다 — 만회할 기회를 주는 쪽이다.

난수는 크기에만 얹고 **부호에는 얹지 않는다.** "잘 뒀는데 사이가 나빠졌다"는 배울 것이
없는 결과다.

### 11.3 설정으로 넘기는 것

우호도는 사람마다 셈이 다른 값이라 크기를 우리가 못 박지 않는다(§12).

| 설정 | 기본 | 하는 일 |
|---|---|---|
| `playTogetherGoodwill` | 켬 | 이 절 전체 |
| `playTogetherGoodwillScale` | 100 % | 표의 모든 크기에 곱한다. **0 % 면 사실상 끔** |
| `playTogetherGoodwillDrop` | 켬 | 끄면 마이너스가 0 이 된다 — 오르기만 한다 |

11.2 의 파밍 방지 둘(난이도 곱, 방문 1회당 한 번)은 **설정으로 내리지 않는다.**
끌 수 있게 하면 그건 취향이 아니라 그냥 우호도 자판기다.

방문객이 체류 시간이 끝나 먼저 일어서는 경우는 §8 과 같은 처리에 문구만 다르게 한다.
그때는 우호도를 건드리지 않는다 — 판을 끝낸 것이 아니기 때문이다.

이건 3단계다. 1단계에서는 방문객을 상대로 고를 수만 있게 하고 우호도는 뒤로 미룬다.

### 11.3 우호도와 기분은 함께 움직인다 (2026-09-10)

처음에는 손님에게 생각(기분)을 주지 않았다. "손님의 기분까지 우리가 적을 자리는
아니다" 였는데, 그러면 **손님은 팩션의 숫자이지 한 판 두고 간 사람이 아니다.**
우호도만 움직이고 기분은 안 움직이는 손님은 자판기에 가깝다. 그래서 셋 다 손님에게도
준다 — `PR_PlayedTogether`, `PR_PlayedTogetherSocial`, `PR_OpponentGone`.

공짜로 성립한다. 바닐라 기분 욕구(`Mood`)는 `minIntelligence: Humanlike` 만 걸려 있고
**`colonistsOnly` 가 아니다** — 여가 욕구(`Joy`)와 다른 점이다. 방문객에게도
`needs.mood` 가 그대로 있으므로 우리가 할 일은 팩션 검사를 지우는 것뿐이었다.

### 11.4 손님이 먼저 앉는다 (2026-09-10)

2칸은 여태 **전부 플레이어가 청해야** 시작됐다. 그러면 손님은 상대가 될 수 있는
사람이지 상대를 청하는 사람이 아니다 — 앉아서 기다리는 그림 하나가 그 차이를 만든다.

**퀘스트로 만들지 않는다.** `QuestScriptDef` + QuestNode 그래프 + "이 사람과 한 판
두기" 를 지켜볼 `QuestPart` + 보상 경제가 필요하고, 무엇보다 퀘스트라는 형식이 큰
편지 · 퀘스트 탭 · 기한 · 팩션 이해관계를 함께 데려온다. 체스 한 판 청하는 일이 그
틀에 들어가면 숙제가 된다. 스토리텔러도 `IncidentDef` 도 쓰지 않는다 — 스토리텔러에
붙으려면 바닐라 `StorytellerDef.comps` 를 건드려야 하고 그래도 모드 스토리텔러는 못
잡는다. `GameComponent_Recreation` 이 이미 돌고 있으므로 필요한 것은 주사위 하나다.

| | |
|---|---|
| 언제 | 게임 안 한 시간마다, 기본 8% (설정 배율 0~300%) |
| 누가 | 자격(`Together.Judge`)이 있고, 한가하고(`casualInterruptible`), 떠나는 지시(`ExitMap*`·`TravelOrLeave`)를 받지 않은 손님 |
| 어디 | 승부가 있고, 비어 있고, 두던 판이 남아 있지 않고, 식구가 예약하지 않은 가구 |
| 알림 | 편지가 아니라 **상단 한 줄.** 가구를 가리킨다 |
| 고르기 | 그 가구를 누르면 상대 목록 **맨 위**에 "(기다리는 중)" 으로 뜬다 |
| 안 가면 | 두 시간 뒤 **혼자 둔다** |

**혼자 두는 것이 핵심이다.** 일어나서 가 버리면 청한 적도 없는 일이 되지만, 혼자 두는
손님은 그 자리에 남아 "저 사람 아직 저기 있네" 가 된다. 혼자 두는 방법은 우리가 만들지
않는다 — 그 가구의 `vanillaJob` 이 이미 그것이다. 앉을 자리를 고르는 규칙도 바닐라
`JoyGiver_InteractBuildingSitAdjacent` 를 그대로 따른다(상하좌우 인접 칸, 의자 우선).

시간을 재는 곳은 기다리는 Job 이 아니라 `TogetherInvite.Sweep` 이다. 앞선 보강에서
배운 그대로다 — 손님이 도중에 다른 일을 받으면 그 Job 은 아예 안 돌기 때문에,
안에서 지우게 두면 청이 남는다.

**손님 판은 조용하다는 것을 기억해 둔다** (§16-1 / D6). 이 기능은 그 조용함을 더
눈에 띄게 만든다 — 18.1 나레이션을 결정할 때 이 자리를 함께 본다.

## 12. 설정

**새 최상위 설정은 만들지 않는다.** 이미 있는 몰입 모드 아래로 들어간다(§0).

### 12.1 배치

무효화 블록이 이미 쓰고 있는 문법을 그대로 따른다 — 부모 체크박스, 들여쓴 하위,
조건부 슬라이더. **부모가 꺼져 있으면 하위는 아예 그려지지 않는다.**

```
[게임 진행]
  ☑ 판을 두는 동안 게임을 멈춘다                        ← 이미 있다
  ☑ 몰입 모드 (정착민이 가구까지 걸어간 뒤 시작)         ← 이미 있다. 뜻 그대로
      ☐ 상대를 골라 둘이 함께 둔다                       ← 새 부모
          부를 수 있는 거리          [====----]  30 칸
          ☑ 어린이도 상대가 된다
          ☑ 방문객도 상대가 된다
          ☑ 성격이 판에 비친다
              ☑ 화가 나면 판을 엎을 수 있다
              ☑ 다정한 상대는 무르기를 한 번 더 준다
          ☑ 방문객과 두면 팩션 우호도가 움직인다
              우호도 크기            [=====---]  100 %
              ☑ 나쁘게 끝나면 내려가기도 한다
          ☐ 판의 일을 폰 기록에 남긴다
```

### 12.2 항목

| 항목 | 기본 | 부모 | 왜 설정인가 |
|---|---|---|---|
| `immersionMode` | 꺼짐 | — | **이미 있다.** 뜻도 저장 키도 그대로 |
| `playTogether` | 꺼짐 | `immersionMode` | 2칸 자체. 켜면 `pauseWhilePlaying` 과 상호 배타 (D1), `saveSessions` 가 켜져 있어야 한다 |
| `playTogetherRange` | 30 칸 | `playTogether` | 맵 크기와 정착지 모양에 따라 적정값이 다르다. 없으면 맵 반대편 사람을 두 시간 기다린다 (§10.1) |
| `playTogetherChildren` | 켬 | 〃 | 아이를 판에 앉히는 것에 대한 취향이 갈린다 (§10.1) |
| `playTogetherVisitors` | 켬 | 〃 | 끄면 §11 이 통째로 잠긴다 |
| `playTogetherPersonality` | 켬 | 〃 | §10.2. 끄면 성격을 안 본다 |
| `playTogetherBoardFlip` | 켬 | `…Personality` | **판이 남의 성격 때문에 끝난다.** 사람에 따라 이건 이야기가 아니라 짜증이다 |
| `playTogetherKindUndo` | 켬 | 〃 | 무르기 한 칸은 난이도에 직접 닿는다 |
| `playTogetherGoodwill` | 켬 | `playTogether` | §11 |
| `playTogetherGoodwillScale` | 100 % | `…Goodwill` | 우호도는 전략 자원이다. 0~200 % 로 두고 **0 이면 사실상 끔** |
| `playTogetherGoodwillDrop` | 켬 | 〃 | 마이너스를 아예 안 보고 싶은 사람이 있다 |
| `playTogetherNarration` | **꺼짐** | `playTogether` | 18.1. **기본이 꺼짐인 데는 이유가 있다** — 아래 |
| `invalidateOnOpponentGone` | 켬 | `invalidateSessions` | §8. **새 자리가 아니다** — 이미 있는 무효화 목록의 들여쓴 한 줄로 들어간다 |

관전·응원(18.3)은 그때 `playTogether` 아래 하위 두 줄로 붙는다 — 켜기 하나와 인원 상한
슬라이더 하나. 지금 자리만 비워 둔다.

### 12.3 `playTogetherNarration` 만 기본이 꺼짐인 이유

나머지는 전부 취향이지만 이건 **안전 문제**다. 18.1 에서 확인했듯 우리
`InteractionDef` 로 남긴 기록은 모드를 빼도 세이브에 남고, 바닐라 표시 코드가
`intDef` 를 null 검사 없이 쓴다. 위험이 크진 않지만(기록은 150개로 계속 밀려난다)
**"넣고 빼도 안전하다"는 약속을 우리가 먼저 깨면서 기본값으로 켤 수는 없다.**

켜는 사람은 그 대가로 다른 모드가 판을 읽을 수 있게 된다. **그건 선택지이지
기본값이 아니다.**

### 12.4 "새 설정은 셋을 넘기지 않는다" 를 여기서 접는다

이 문서는 원래 그렇게 적혀 있었다. 뒤집는다.

그 규칙은 **기능이 스위치 하나일 때** 옳았다. 지금 이 기능은 누구를 앉힐지, 얼마나
멀리서 부를지, 성격이 판을 끝내도 되는지, 우호도가 내려가도 되는지 — **취향이 갈리는
지점을 여럿 갖고 있다.** 그런 것을 코드가 정하면 설계가 깔끔한 게 아니라 그냥 남의
취향을 대신 정한 것이 된다.

대신 규율을 다르게 건다.

1. **최상위는 늘지 않는다.** 전부 `immersionMode` → `playTogether` 아래다. 2칸을 안 켠
   사람의 설정 창은 **지금과 한 줄도 다르지 않다.**
2. **기본값만 두면 설계한 그대로다.** 아무것도 안 만져도 §0~§11 이 말한 그 기능이다.
3. **끄는 쪽이 항상 안전하다.** 어떤 항목을 꺼도 기능이 부서지지 않고 더 조용해질 뿐이다.
   유일한 예외인 `playTogetherNarration` 은 그래서 **켜는 쪽**이 선택이다.

## 13. 번역 키 (EN·KO 양쪽)

`verify.py` 가 짝과 미사용을 잡는다. 접두사는 `PR.Together.*` 로 통일한다 —
"reality" 는 이제 쓰지 않는 이름이다(§0).

**판 안에서 (10짝)**

```
PR.Together.PickOpponent      상대 고르기
PR.Together.Waiting           X 를 기다리는 중
PR.Together.NoOpponent        올 수 있는 사람이 없다
PR.Together.CannotPlay        X 는 지금 앉을 수 없다 (이유)
PR.Together.LeftForNeed       X 가 일어섰다 (배고픔·피로)
PR.Together.Header            맞은편 이름표
PR.Together.Talk              기록 패널의 사교 띠 제목
PR.Invalidation.OpponentGone.Label   무효화 목록의 한 줄 (기존 형식에 맞춘다)
Thought_PR_OpponentGone       생각 이름·설명
Thought_PR_PlayedTogether     〃
```

**설정 창에서 (12짝 + 설명 4짝)**

```
PR.Settings.Together                라벨 + .Desc
PR.Settings.Together.Range          "부를 수 있는 거리: {0} 칸"   ← 값 하나 들어간다
PR.Settings.Together.Children       라벨
PR.Settings.Together.Visitors       라벨
PR.Settings.Together.Personality    라벨 + .Desc
PR.Settings.Together.BoardFlip      라벨 + .Desc
PR.Settings.Together.KindUndo       라벨
PR.Settings.Together.Goodwill       라벨
PR.Settings.Together.GoodwillScale  "우호도 크기: {0} %"          ← 값 하나
PR.Settings.Together.GoodwillDrop   라벨
PR.Settings.Together.Narration      라벨 + .Desc                  ← 설명이 특히 중요하다
PR.Settings.Together.NeedsSave      "세션 저장이 켜져 있어야 합니다"  ← 회색일 때의 이유
```

**설명(`.Desc`)을 다는 기준**: 켜고 끄는 결과가 이름만으로 안 보이는 것에만 단다.
`BoardFlip` 과 `Narration` 은 반드시 필요하다 — 하나는 판이 남의 성격으로 끝난다는
뜻이고, 하나는 세이브에 자국이 남는다는 뜻이다.

## 14. 제약 점검 (`HANDOFF.md` §2)

설정이 늘어난 것은 제약과 무관하다 — 전부 `PRSettings` 의 필드와
`Scribe_Values.Look` 이고, 새 Def 도 Harmony 도 없다. 다만 저장 키가 늘어나므로
**기존 키의 이름과 뜻은 하나도 건드리지 않는다**(특히 `immersionMode`).


| 제약 | 이 명세는 |
|---|---|
| 1. Harmony 0 개 | 지킨다. 새 JobDef·JobDriver·기존 Comp 확장뿐이다 |
| 2. 바닐라 Def 수정 없음 | 지킨다. 새 Def 는 전부 우리 것 |
| 3. `Core/`·`AI/` 는 Verse 를 모른다 | **건드리지 않는다.** 이 기능은 전부 `Framework/` 다 |
| 4. `Framework/` 는 게임을 모른다 | 지킨다. 상대 폰은 게임이 아니라 수명의 개념이다 |
| 5. 게임 속도를 건드리지 않는다 | **D1 이 이 제약에서 나왔다.** `CurTimeSpeed` 를 읽지도 쓰지도 않는다 |
| 6. `packageId` 불변 | 무관 |
| 7. `*SaveData` 이름 불변 | 지킨다. `GameSession` 은 `*SaveData` 가 아니다 |

**제약 3 이 이 기능의 크기를 정한다.** 상대 폰은 규칙이나 AI 가 아니라 수명에 속하므로
게임 코드는 한 줄도 바뀌지 않는다. 아홉 게임 전부가 공짜로 이 기능을 얻는다.

## 15. 단계

| 단계 | 내용 | 이게 끝나면 |
|---|---|---|
| **1** ✅ | 상대 고르기 + 머리글 이름 + 제안 난이도. **붙잡지 않는다**<br>· 세 진입점을 한 문으로 모으기 (§4) ✅<br>· 같은 사람을 두 판에 지목하지 못하게 하기 ✅<br>· 설정 네 줄 ✅ | ferny 의 지적이 해결된다. 굶주림 문제가 아예 없다 |
| **2** ✅ | 두 폰 붙잡기 — 예약, `Hold` 토일, 욕구 탈출구, 창·폰 결합 ✅<br>· 상대 폰 예약 + 실패 시 양쪽 되돌리기 (§6.1) ✅ — **붙잡을 때 비로소 뜻이 생긴다**<br>· 설정: `…Personality` (성격은 여기서 저절로 따라온다) ✅ | "실제로 앉아 있다"가 된다 |
| **3** ✅ | `OpponentGone` + 생각 ✅. 방문객 우호도 ✅<br>· 설정: `invalidateOnOpponentGone` · `…Goodwill` 3줄 ✅ | 이야기가 닫힌다 |
| **4** ✅ | 관계 반영 ✅, 마주 보게 세우기 ✅, 성격 두 조각(다정다감 무르기 · `Losing`) ✅<br>· 설정: `…BoardFlip` · `…KindUndo` ✅ | 성격의 나머지는 2단계에 이미 따라와 있다 (§10.2) |

**1단계는 2026-09-09 에 들어갔다.** 문서가 말한 그대로 아무도 붙잡지 않는다 —
상대에게 이름과 얼굴만 생겼고, 굶주림 문제는 구조적으로 없다.
§6.1 의 예약은 2단계로 옮겼다 — 붙잡지 않으면 예약할 것이 없기 때문이고,
대신 "같은 사람을 두 판의 상대로 지목하지 못하게" 만 먼저 넣었다.

**2단계도 같은 날 들어갔다.** `TogetherMatch` · `TogetherToils` · `JobDriver_JoinGame`
셋이 새로 생겼다. 진행 상태를 세이브에 남기지 않은 것이 설계다 — 창이 세이브에 남지
않으므로 불러온 판에는 붙잡을 이유도 없고, 그래서 저장·불러오기 중에 폰이 영원히 서
있는 그림이 구조적으로 생기지 않는다. 자기 치유가 공짜로 따라온다.

2단계에서 명세가 한 군데 틀린 것이 드러났다. §6.1 이 `maxPawns: 2` 를 "바닐라가 2인용
가구에 쓰는 관용" 이라고 적었는데, 실제 `joyMaxParticipants` 는 체스·당구·우르 2,
편자·후프스톤 3, 포커 4, 망원경 1 로 제각각이다. 2 로 고정했으면 포커 테이블에서
바닐라 폰이 그 가구를 아예 못 쓰게 됐을 것이다 — `Together.SeatCount` 가 게임의
`vanillaJob` 에서 그 값을 그대로 가져온다. §16-3 의 답이기도 하다.

**3단계에서 명세를 한 번 좁혔다.** §8 은 무효화 축을 "참가자"로 잡으라고 했지만,
그대로 쓰면 **2칸을 안 켜둔 사람의 판까지 무효화된다** — 혼자 두던 판을 남겼는데 그
정착민이 캐러밴을 나가면 판이 사라지고 전적에 무효가 하나 찍힌다. 그래서 이 축은
`opponentPawn` 이 있는 판에서만 재고, 그 안에서는 명세대로 양쪽을 다 본다.

**4단계의 "관계 반영"은 두 줄로 읽었다.** 상대 고르기 목록을 의견순으로 세우고
(식구가 열다섯이 넘으면 목록이 이름 더미가 된다), 한 판을 끝까지 둔 둘은 서로에 대한
의견이 조금 오른다(`PR_PlayedTogetherSocial`). 둘 다 판의 규칙을 건드리지 않는다.

**`Losing` 은 체스와 우르만 답한다.** 명세가 말한 대로 "답할 수 있는 게임만 답한다" 이고,
나머지 일곱은 기본값 null 로 둔다 — 판세를 모르는 게임에서는 판을 엎지 않는다.
체스는 `ChessEval.Evaluate` 가 마이너스 하나치 밖이면, 우르는 말이 둘 이상 뒤지면 지고 있는 것이다.

**1단계만으로도 낼 수 있었다.** 그게 이 순서의 요점이었다 — 2단계가 늦어져도 1단계가
사용자에게 닿는다. 반대로 1단계 없이 2단계는 뜻이 없다.

### 네 단계가 끝난 뒤의 보강 (2026-09-10)

네 단계를 다 넣고 나서 코드를 다시 읽어 세 군데를 고쳤다. 셋 다 "판이 어긋나면
스스로 못 돌아오는" 자리였다.

- **부르다 만 판이 영영 남는다.** 치우는 일을 `Hold` 토일의 마무리 동작이 맡고
  있었는데, 바닐라 `JobDriver.Cleanup` 은 **그때 실행 중이던 토일 하나의** 마무리만
  부른다. 걸어가는 도중에 징집되면 `Hold` 는 시작조차 안 했으므로 마무리도 없고,
  둘 다 그렇게 빠지면 그 두 사람이 계속 "이미 다른 판에 있음" 으로 걸려 다시는
  상대로 못 뽑힌다. `TogetherMatch.Sweep()` 을 밖에서 1초마다 돌린다.
  같은 고침이 **연습 판의 "다시 두기"** 도 살렸다 — 새 부름이 옛 Job 의 마무리에
  지워지고 있었다.
- **불러온 판을 못 오는 상대로 연다.** 세션은 상대를 그대로 들고 있어서 혼자 여는
  길이 없는데도 창을 열고 있었다. §7.1 이 반 초 만에 다시 닫아 "못 온다" 다음에
  "자리를 떴다" 가 잇따랐다. 이제 아무것도 열지 않고 판을 가구에 남긴다.
- **한 판 더 두면 우호도 문구가 뒤집힌다.** 한 방문에 여러 판을 두면 앞 판에서 준
  만큼을 빼고 갈아 끼우므로, 좋은 판을 두고도 숫자가 내려갈 수 있다. 문구를 이번
  판의 값으로 고르고 있어서 "올랐다" 밑에 내려간 두 숫자가 붙었다.

그리고 메인메뉴로 나갔다 들어와도 `TogetherMatch` 의 static 목록이 지난 판을 그대로
들고 있던 것을 `StartedNewGame` · `LoadedGame` 에서 놓게 했다.

1단계에 딸린 두 줄은 §19 에서 올라온 것이다. 나중에 붙일 수 있는 것이 아니다 —
기즈모로 열면 상대 고르기를 건너뛰고, 상대를 예약하지 않으면 같은 사람이 두 판에
지목된다. 둘 다 **1단계의 약속을 직접 깨는 구멍**이라 같은 단계에 있어야 한다.

## 16. 확인해야 할 가정 (인게임)

만들기 전에 테스트 식민지에서 봐야 하는 것들. 여기서 틀리면 명세가 바뀐다.

1. ~~**바닐라 사교 상호작용이 정말 알아서 굴러가는가** (D6).~~ — **코드로 답이 나왔다
   (2026-09-10).** 사슬이 전부 이어진다. `Pawn_InteractionsTracker` 를 디컴파일해
   한 칸씩 확인했다:

   | 고리 | 실제 | 우리 쪽 |
   |---|---|---|
   | 사교 모드 | `CurrentSocialMode` = `curDriver.DesiredSocialMode()` = **지금 토일의 `socialMode`** | `TogetherToils.Hold` 가 `Normal` 로 둔다 ✔ |
   | 굴리는 주기 | 폰마다 60틱마다 `Rand.MTBEventOccurs(6600, 1, 60)` | 둘이 붙어 있으므로 **게임시간 1분 안팎에 한 번꼴** |
   | 연달아 막기 | `lastInteractionTime + 320` · `InteractedTooRecentlyToInteract` 은 +120 | 판 하나에 대여섯 줄이 상한이라 맞다 |
   | 자리 | `IsGoodPositionForInteraction` = 6칸 이내 + 시야 | 가구를 사이에 두고 붙어 선다 ✔ |
   | 후보 | `mapPawns.SpawnedPawnsInFaction(pawn.Faction)` — **같은 팩션만** | §19 에 이미 적혀 있다. **방문객과는 한 줄도 안 뜬다** |
   | 남는 곳 | `TryInteractWith` 이 `PlayLogEntry_Interaction` 을 `Find.PlayLog.Add` | §7.2 의 `CollectTalk` 이 그대로 집는다 ✔ |

   그러므로 §7.2 는 식구끼리 둘 때 굴러가고, 손님과 둘 때는 빈다. **그 빈자리가
   18.1 나레이션이 필요한 이유다** — 손님 판은 다른 것으로 채우지 않으면 조용하다.
   눈으로 볼 것은 이제 "정말 뜨는가" 가 아니라 "그 속도가 읽기 좋은가" 뿐이다.
2. ~~**`SkillRecord.Level` 이 유전자 적성을 포함하는가.**~~ — **묻지 않기로 했다
   (2026-09-10).** 유전자 적성은 어차피 스탯으로 들어간다. 들어간 값이 제안 난이도에
   비치는 것은 D2 가 말하는 그대로이고, 굳이 우리가 따로 셀 이유가 없다.

   대신 **반대쪽 구멍**을 메웠다. 배경 이야기가 오락을 **직접** 말하는 경우다 —
   `ChessMaster67`("체스 마스터")의 바닐라 보상은 지능 2 인데 연구원 배경은 지능 6 이라,
   스킬만 보면 **체스는 연구원이 더 잘 둔다.** 이름이 체스 마스터인 사람을 두고서.
   그래서 `MiniGameDef.backstoryAffinities` 를 두어 **그 게임에서만** 연동 스킬에
   레벨을 얹는다. 바닐라 배경 전부를 훑어 직접 말하는 것만 골랐다:

   | 배경 | 게임 | 얹는 레벨 | 근거 |
   |---|---|---|---|
   | `ChessMaster67` (체스 마스터) | 체스 | +6 | 제목이 곧 그 놀이다 |
   | `BlackjackPlayer76` (블랙잭 플레이어) | 포커 | +6 | 카드 세는 법을 배웠다 |
   | `AmateurAstronomer77` (아마추어 천문학자) | 별 보기 | +6 | 망원경으로 몇 시간씩 봤다 |
   | `RebelStudent14` (반항적인 학생) | 체스 | +3 | "체스를 두고 스키트를 쏘도록" — 했지만 그게 전부다 |

   **넣지 않은 것이 더 중요하다.** `Pirate56` 은 "체스 같은 함정"이라는 비유일 뿐이고,
   `ScienceProdigy65` 의 천문학은 학교 과목 나열 중 하나다. 공학자·연구자처럼 오락을
   말하지 않는 배경은 지능이 알아서 올라가므로 이미 반영되어 있다 — 목록에 넣기
   시작하면 배경 이야기 전체를 우리가 다시 채점하게 된다.

   `linkToPawnSkill` 이 꺼져 있으면 이것도 안 쓴다. 제안 난이도를 안 쓰겠다는 뜻이므로
   따로 설정을 늘리지 않았다. 상대 고르기 목록에는 `홍길동 (체스 마스터)` 로 적힌다 —
   그 사람을 고르면 난이도가 뛰는데 왜 뛰는지가 안 보이면 그냥 이상한 일이 된다.
3. ~~**`maxPawns: 2` 예약이 바닐라 여가 가구에서 통하는가.**~~ — **코드로 답이 나왔다.**
   통하지 않는다. 값이 게임마다 다르므로(2·3·4·1) 고정하면 안 되고, 바닐라와 같은
   수를 써야 한다. `Together.SeatCount` 로 고쳤다. 다른 모드의 Job 과 부딪히는지는
   여전히 인게임에서 봐야 한다 — `MaxPawns` 가 다르면 코드상 즉시 거절이다.
4. **가구 앞에 두 폰을 세울 자리가 있는가.** 마주 보게 하는 것은 나중 문제지만,
   1×1 가구(펀칭백)는 애초에 2인이 성립하지 않는다 — `MiniGameDef` 에
   "둘이 할 수 있는 게임인가"를 두어야 할 수도 있다.
5. ~~**사교 다툼이 났을 때** 판을 무효화할지 그냥 둘지.~~ — **4단계에서 정했다.**
   우리가 엎은 판(`playTogetherBoardFlip`)은 세션을 버리고 창을 닫는다 — 엎는다는 게 그 뜻이다.
   다만 전적에는 적지 않는다. 기권한 것은 플레이어가 아니므로 패배가 아니라 중단이다(§7.1 과 같다).
   바닐라가 알아서 낸 다툼은 그대로 둔다 — 그건 우리가 만든 사건이 아니기 때문이다.
6. **붙잡는 동안 대화 빈도가 볼 만한가.** 코드상 `Hold` 토일의 기본 `socialMode` 는
   `Normal` 이라 같은 팩션끼리는 가능하지만, **얼마나 자주 뜨는지와 좌석별 시야**는
   돌려 봐야 안다. 너무 뜸하면 §7.2 의 창이 대부분 비어 있게 된다 — 그러면 18.1 의
   내보내기가 선택이 아니라 필수가 된다.

## 17. 리스크

- **콜로니가 선다.** 여섯을 세 탁자에 붙잡아 두면 일이 멈춘다. → **2단계에서 상한을
  두지 않기로 했다.** 판은 언제나 플레이어가 직접 한 판씩 시작하는 것이라 저절로
  늘어나지 않고, 배고픔·피로는 D4 가 알아서 자리를 뜨게 한다. 상한은 "왜 이 사람은
  못 앉는가"라는 설명할 수 없는 거절을 하나 더 만들 뿐이다.
- **예약 충돌.** 다른 모드가 같은 가구를 예약하면 조용히 실패해야 한다.
- **1×1 가구.** 펀칭백·슬롯은 2인이 성립하지 않는다. `hasMatch` 와 별개로
  "2인 가능" 표시가 필요할 수 있다(§16-4).
- **기능이 커 보인다.** 1단계만 내면 작다. 2단계부터가 진짜 비용이다.

## 18. 다음 층 — 아직 결정하지 않은 확장

셋 다 §0~§17 을 **바꾸지 않고 그 위에 얹는다.** 크기는 서로 다르다 — 18.2 는 이 기능을
"두 사람이 앉는다"에서 **"여럿이 모인다"** 로 바꾸는 큰 얹기고, 18.1 과 18.3 은 붙잡는
사람을 늘리지 않는 작은 얹기다. 1단계는 셋 다와 무관하게 그대로 유효하다.

### 18.1 상황 서술 내보내기 — 특정 모드에 붙이지 않는다

§7.2 가 **들여오기**(바닐라의 말 → 우리 창)라면 이쪽은 **내보내기**(우리 사건 → 바닐라 기록)다.

우리가 쓰는 것은 대사가 아니라 **상황 서술**이다. 바닐라 기록이 하는 그 말투 — "누가
무엇을 했다". 그러면 대사를 짓는 일은 읽는 쪽으로 넘어가고, 우리는 아홉 게임 × 두
언어의 대본을 쓰지 않아도 된다.

**특정 모드에 연결하지 않는다.** 바닐라 자리에 바닐라 방식으로 남겨 두면 그만이고,
읽는 쪽은 읽는 쪽 사정이다. 아래 확인은 "붙일 수 있나"가 아니라 **"그냥 남겨 두는 것만으로
충분한가"** 를 본 것이다.

#### 명제 정정 — 게임 상태를 모르는 것은 **들어오는 쪽**뿐이다

D6 을 "대화는 게임 상태를 모른다"로 읽으면 이 절과 부딪힌다. 정확히는 방향마다 다르다.

| 방향 | 게임 상태를 아나 | 근거 |
|---|---|---|
| 들여오기 (§7.2) | **모른다** | 바닐라가 굴리는 잡담이다. 우리는 창에 얹기만 한다. 그래서 대본이 필요 없다 |
| 내보내기 (18.1) | **안다. 그게 요점이다** | 바깥이 판이 있었다는 걸 아는 통로가 이것뿐이다 |

**제약 4 는 여전히 지켜진다.** 제약 4 는 *코드* 의존이지 정보 차단이 아니다. 내보낼 문장에
들어가는 게임 이름은 `MiniGameDef.label` 이라는 **데이터**에서 오고, `Framework/` 는 여전히
어떤 게임의 코드도 모른다. `playThought` · `vanillaJob` 이 이미 그렇게 하고 있다.

#### 매 수가 아니라 마디에만 — 그리고 그 마디를 게임이 알려 준다

**한 수 둘 때마다 내보내지 않는다.** 이유가 셋인데 하나는 취향이고 둘은 사실이다.

1. **폰 기록은 150줄이 전부다.** `PlayLog.ReduceToCapacity` 가 `while (entries.Count > 150)`
   로 뒤에서부터 버린다. 체스 한 판을 수마다 흘려보내면 **정착지가 지난 며칠 동안 무슨
   말을 하고 누구와 다퉜는지가 우리 기보에 밀려 사라진다.** 남의 기억을 지우면서 우리
   이야기를 적는 셈이다.
2. **어차피 못 나간다.** 상호작용은 120틱 쿨다운이 있고(§ 위), 읽는 쪽에도 쿨다운이 있다.
   많이 쏘면 더 나오는 게 아니라 그냥 버려진다.
3. 마디만 남아야 **읽었을 때 이야기로 읽힌다.** 스무 줄짜리 기보는 아무도 안 읽는다.

**프레임워크가 아는 마디 (게임이 말 안 해도 안다)**

| 마디 | 언제 |
|---|---|
| 판이 시작됐다 | 둘 다 앉았을 때. 게임 이름 · 난이도 · 몇 판째 |
| 판이 끝났다 | 승패 · 기권 · 중단(배고픔 등) · 상대 소멸 |

**게임만 아는 마디 (게임이 올려 준다)**

여기가 사용자가 말한 "상대 말을 잡았을 때 · 역전했을 때 · 큰 실수했을 때"다. 그런데
**프레임워크는 비숍이 뭔지 몰라야 한다**(제약 4). 그래서 게임이 *무슨 일*을 올리는 게
아니라 **어떤 종류의 순간**인지만 올린다.

```csharp
/// <summary>이야기가 될 만한 순간. 게임이 규칙을 알고, 프레임워크는 종류만 안다.</summary>
public enum MatchBeat { Gain, Loss, Turnaround, Blunder }

/// <summary>이번에 이야기가 될 일이 있었나. 없으면 null. 한 번 가져가면 지워진다.</summary>
public virtual MatchBeat? TakeBeat() { return null; }
```

| 종류 | 체스에서 | 포커에서 | 다트에서 |
|---|---|---|---|
| `Gain` | 상대 말을 잡았다 | 큰 팟을 가져갔다 | 트리플 20 |
| `Loss` | 내 말이 잡혔다 | 큰 팟을 잃었다 | — |
| `Turnaround` | 뒤지다 앞섰다 | 〃 | 〃 |
| `Blunder` | 큰 실수 | 무리한 콜 | 버스트 |

**표의 오른쪽은 게임 안에 있고 프레임워크는 왼쪽만 본다.** `HasProgress` · `Losing` 과
같은 패턴이다 — 기본값은 "말할 것 없음"이고, 답할 수 있는 게임만 답한다. 별 보기·슬롯
처럼 승부가 없는 것은 아예 해당이 없다.

**상한을 건다.**

- **한 판에 여섯 줄까지.** 시작·끝은 늘 나가고, 가운데 마디는 최대 넷이다.
- **같은 종류는 판당 한 번.** 역전이 세 번 나도 첫 역전만 나간다. 실수도 마찬가지다.
- 넘치면 **버린다.** 밀어내지 않는다 — 뒤에 더 좋은 마디가 올지 모른다고 기다리지도 않는다.

150줄 중 여섯이면 4%다. 그 정도가 남의 기억을 밀어내지 않고 우리 이야기를 남기는 선이다.

| 그래도 내보내지 않을 것 |
|---|
| 체스의 수, 포커의 패 같은 **게임별 세부** — 종류만 나가고 내용은 안 나간다 |
| `MiniGameWorker.Log` 의 UI 용 짧은 줄 — 그건 창 안에서 볼 것이다 |
| 상한을 넘긴 마디 |

#### 확인한 것 (림톡 `cj.rimtalk` 3551203752, 1.6 어셈블리)

| 확인 | 결과 |
|---|---|
| 남의 상호작용을 읽나 | **읽는다.** 설정 "Override All Interactions with AI Dialogue" 의 툴팁이 *"including vanilla RimWorld and other mods"*. 어셈블리에 `ProcessNonRimTalkInteractions` |
| 기록을 읽나 | **읽는다.** `get_PlayLog` |
| 폰이 뭘 하는지 어떻게 아나 | 프롬프트에 `Activity:` 칸이 있고 `GetReport` · `get_CurJob` · `get_CurJobDef` 를 쓴다 |
| 모임도 보나 | 프롬프트 문자열에 `LordJob_` 이 있다 |

**결론: 우리 쪽에 림톡을 아는 코드는 필요 없다.** 바닐라 자리에 남겨 두면 읽어 간다.

#### 그래서 실제로 할 일은 두 줄이다

1. **우리 `InteractionDef` 를 만들어 `pawn.interactions.TryInteractWith(상대, def)` 로 쏜다.**
   `logRulesInitiator` 에 서술 문장을 넣으면 바닐라 `PlayLogEntry_Interaction` 이 그것을 싣는다.
   Harmony 0 개(우리 토일에서 부르는 공개 호출), 바닐라 Def 수정 0 개.
   - 어떤 `InteractionDef` 를 쓸지는 `MiniGameDef` 의 새 필드로 둔다. **데이터라서 제약 4 가 안 깨진다.**
   - `socialFightBaseChance` 는 **바닐라 필드다.** 사교적 다툼은 코드가 아니라 숫자 하나다.
     `initiatorThought` · `recipientThought` · `initiatorXpGainSkill` 도 마찬가지로 공짜다.
   - **실패 조건이 쿨다운만이 아니다.** `CanInteractNowWith` 가 보는 것: 120틱 쿨다운, 상대가
     `Spawned`, **6칸 이내 + 시야**(`IsGoodPositionForInteraction`), 발신·수신 자격(깨어 있음 ·
     말할 수 있음 · 상호작용 차단 아님). 마주 앉은 두 사람은 대개 통과한다 — 바닐라 폰이
     식탁 너머로 늘 떠드는 것과 같은 조건이다. 그래도 **실패는 정상이다.** 판의 마디에서만
     쏘고, 실패하면 그 마디는 조용히 넘긴다 — 재시도로 매달리지 않는다.
   - 마디가 무엇인지는 위 절에서 정했다. **`TakeBeat()` 이 null 이면 아무것도 안 한다.**
2. **우리 JobDriver 의 `GetReport()` 를 덮어쓴다.** 지금은 `heading over to play.` 한 줄이다.
   붙잡는 토일이 생기면 "누구와 무슨 게임을 하는 중"이 되어야 한다. **가상 함수 하나이고
   가장 싼 개선이다.**

> **`LogEntry` 하위 클래스는 만들지 않는다.** 세이브에 클래스 이름이 박혀서 모드를 빼면
> 그 항목을 못 읽는다. 림톡은 실제로 그 길을 갔고(`PlayLogEntry_RimTalkInteraction`,
> `SaveGamePatch`), 번역 파일에 *"Failed to save/load interaction texts. Resetting data to
> prevent save corruption."* 라는 문구까지 있다. 그 대가를 우리는 치르지 않는다.

> **정정 — `InteractionDef` 쪽도 "조용히 null" 이 아니다.**
> `PlayLogEntry_Interaction.ExposeData` 는 `Scribe_Defs.Look(ref intDef, "intDef")` 다.
> 모드를 빼면 참조는 null 이 되지만 **항목은 세이브에 그대로 남고**, `GetSymbol` ·
> `GetTipString` · `ToGameStringFromPOV_Worker` · `ToString` 이 `intDef.logRulesInitiator` ·
> `intDef.LabelCap` · `intDef.label` 을 **null 검사 없이 쓴다.** `PlayLog.ExposeData` 에
> 걸러 내는 코드도 없다. 커스텀 `LogEntry` 보다는 낫지만 **안전하지는 않다.**
>
> 완화 요인이 하나 있다 — `PlayLog` 는 150개만 남기고 계속 밀려나므로 노출은 "빼기
> 직전에 둔 판" 정도로 한정된다. 그래도 **"넣고 빼도 안전하다"는 약속에는 못 미친다.**
> 낼 거면 정리 전략을 같이 정해야 한다: 판이 끝날 때 우리가 남긴 항목을 지우거나,
> 서술을 우리 창에만 남기고 `PlayLog` 를 아예 안 건드리거나.
> **이건 아직 열린 질문이다. 앞서 닫힌 것처럼 쓴 것은 틀렸다.**

**설정으로 두고, 기본은 꺼 둔다**(`playTogetherNarration`, §12.3). 남는 자국이
세이브에 걸리는 유일한 항목이라, 켜는 쪽을 선택으로 만든다.

**부수 효과 하나.** 내보내기를 `TryInteractWith` 로 하면 그 결과가 바닐라
`PlayLogEntry_Interaction` 이므로, **§7.2 의 읽는 코드가 우리 사건도 그냥 집는다.**
들여오기와 내보내기가 같은 관을 양방향으로 쓴다 — 별도 경로가 없다.

### 18.2 모으기 — 의식이 아니라 **코어의 모임** 뼈대로

의식 뼈대는 쓸 수 없다. `RitualBehaviorDef` 인스턴스가 **코어에 0개**이기 때문이고,
우리는 DLC 없이 도는 모드다.

> **정정.** "이데올로기 전용"이라고 썼던 것은 틀렸다. 실제 분포는 이데올로기 12 ·
> 로열티 2 · 바이오텍 1 · 오디세이 1 이다. 여러 DLC 가 함께 쓰는 프레임워크이고,
> **코어에만 없다.** 결론은 같지만 이유가 다르다.

**코어에 같은 모양이 있다.** `Core/Defs/GatheringDefs/Gatherings.xml` 의 `GatheringDef` —
`Party` 와 `MarriageCeremony` 둘이다. `duty` · `workerClass` · `gatherSpotDefs` 로
사람을 모으고 데려오고 붙잡고, 끝나거나 취소되는 것까지 그 안에 있다.

그중 **결혼식이 우리 모양이다.** 파티는 "여럿이 자유롭게" 지만, 결혼식은
**주인공 둘이 정해진 자리에 서고 나머지가 본다** — 그게 정확히 우리가 그리는 그림이다.
`MarriageSpot` 이 `spectateDistance` · `allowedSpectateSides` 를 쓰는 것도 그래서다.

> **정정 — Def 만 바꿔 재사용할 수 있는 게 아니다.** `GatheringWorker` 는 추상 클래스이고
> `TryFindGatherSpot` · `CreateLordJob` 두 개가 필수 구현이다.
> `GatheringWorker_MarriageCeremony` 는 약혼 관계를 직접 읽어
> `LordJob_Joinable_MarriageCeremony` 를 만들고 `RCellFinder.TryFindMarriageSite` 까지
> 결혼에 묶여 있다. **재는 대상은 "Def 하나"가 아니라 새 `GatheringWorker` +
> `LordJob` + `LordToil` 세 벌이다.**

그래도 잴 값은 있다. 되면 **§6 의 커스텀 `Hold` 토일이 통째로 필요 없어질 수 있다.** 모으기·데려오기·
붙잡기·중단이 이미 그 안에 있고, 파티는 원래 사교 상호작용이 일어나라고 만든 장치라
D6 도 공짜로 더 잘 굴러간다.

**그래서 2단계를 손으로 만들기 전에 이 뼈대부터 재 본다.** 재사용이 되면 2단계가 훨씬
작아지고, 안 되면 §6 대로 간다. 순서를 이렇게 두는 것이 이 항목의 요점이다.

### 18.3 관전과 응원 — 붙잡지 않는다, 여가다

**관전은 의무가 아니다.** 부르지도, 모으지도, 붙잡지도 않는다. 여가가 필요한 폰이
지나가다 구경하고, 그 구경으로 여가를 채우고, 다 차면 간다. 그게 전부다.

이 문장 하나로 18.2 와 갈라진다. 18.2 는 **둘 사람**을 모으는 이야기지만 관전은 모으기가
아니다. 뼈대도 다르고, 순서도 서로 묶이지 않는다.

**이 모양은 코어에 이미 있다 — 텔레비전이다.**

| 조각 | 코어 위치 |
| --- | --- |
| `JoyGiver_WatchBuilding` | `Core/Defs/Joy/JoyGivers.xml` (호스슈·후프스톤이 쓴다) |
| `JobDriver_WatchBuilding` | `Core/Defs/JobDefs/Jobs_Joy.xml` |
| 설 자리 계산 | `WatchBuildingUtility.CalculateWatchCells` · `TryFindBestWatchCell` |
| 인원 상한 | `JobDef.joyMaxParticipants` — 바닐라 필드지만 **우리는 못 쓴다**(아래 예약 항목) |
| 지속·기술·이펙터 | `joyDuration` · `joySkill` · `joyXpPerTick` · `building.effectWatching` |

텔레비전이 하는 일이 정확히 이것이다. 여가 욕구가 온 폰이 스스로 걸어와, 정해진 거리
안에 서서, 시간만큼 여가를 얻고, 욕구가 차거나 더 급한 일이 생기면 떠난다. 새로 설계할
것이 거의 없다.

- **§17 의 "콜로니가 선다" 가 여기서는 거의 사라진다.** 관전하는 폰은 **어차피 여가를 쓰고
  있었을 폰**이다. 체스판 뒤에 서 있든 텔레비전 앞에 앉아 있든 노동 시간이 줄지 않는다.
  앞서 "관전자는 그 위험을 키우므로 인원 상한이 필수"라고 적었던 것은 관전자를 불러
  모은다고 가정했을 때의 이야기다. 자발적 여가로 두면 상한은 **성능·연출용 위생**이지
  안전장치가 아니다.
- **2칸과 독립이다.** 관전은 폰을 붙잡지 않으므로 §6 의 `Hold` 도, §7 의 결합도
  필요 없다. 2칸이 꺼져 있어도 성립한다. 다만 아래의 진행 중 레지스트리가
  선행 조건으로 붙었으므로, **"가장 싸다"고는 더 이상 말할 수 없다**(§19).
- **관전자는 가구를 예약하면 안 된다.** 여기가 이 절에서 제일 위험한 자리다.
  `JobDriver_WatchBuilding` 은 `pawn.Reserve(targetA, job, job.def.joyMaxParticipants, …)` 로
  **가구 자체를 예약한다.** 그런데 `ReservationManager` 는 같은 대상·같은 레이어에 이미
  걸린 예약의 `MaxPawns` 가 지금 요청과 다르면 **개수와 무관하게 즉시 거절**하고, 같아도
  이미 찼으면 거절한다. §6.1 의 참가자 둘이 `maxPawns: 2` 로 가구를 잡고 있으므로
  **바닐라 관전 Job 을 그대로 쓰면 관전자는 0명이 된다.**
  - 어차피 우리 JobDriver 를 쓸 참이므로 답은 간단하다. **관전자는 자기가 설 칸만
    예약하고 가구는 예약하지 않는다.** 대신 `joyMaxParticipants` 가 주던 공짜 상한을
    잃으므로, 상한은 우리가 직접 센다. 위생용이니 그걸로 충분하다.
  - 커스텀 `ReservationLayerDef` 로 층을 나누는 길도 있다. 우리 Def 라 제약 2 는 깨끗하다.
    다만 안 잡아도 되는 것을 잡는 셈이라, 먼저 시도할 쪽은 아니다.
- **설 자리는 우리가 계산한다.** `watchBuilding*` 값은 ThingDef 의 `<building>` 안에 있어서
  `TryFindBestWatchCell` 은 실제 가구 Def 를 읽는다. 남의 가구에 태그를 얹지 않는다 —
  가구 둘레 사각형을 직접 구하면 되는 단순 기하이고, 그래야 제약 2 가 깨끗하다.
  (`CalculateWatchCells` 는 `Thing` 이 아니라 `ThingDef` 를 받으므로 기하값만 담은 우리
  ThingDef 를 넘겨 재사용하는 우회도 있다. 쓰지도 않을 ThingDef 를 게임에 하나 더
  등록하는 값이라, 직접 계산과 견줘 보고 정한다.)
- **볼 것이 있어야 한다 — 그런데 "지금 두는 중"을 물을 데가 없다.** 판이 돌고 있지
  않으면 관전이 성립하지 않는데, 저장된 `GameSession` 의 존재로는 판단할 수 없다.
  `saveSessions` 를 끄면 세션이 아예 안 남고, 진행 중 표시는 `GameComponent_Recreation.ActiveSession`
  **하나뿐**이라 창이 둘 열리면 마지막 것만 남는다. **가구별 "지금 두는 중" 레지스트리가
  먼저 필요하다** — 저장용 세션과는 별개다. 이건 관전보다 앞선 선행 작업이다. §7.2 의 창 안의 말과 18.1 의
  나레이션이 여기서 값을 한 번 더 한다 — 구경거리의 내용이 그쪽에서 나온다.
- **여가 종류**는 게임을 따라간다. 체스를 구경하면 `Gaming_Cerebral`, 다트를 구경하면
  `Gaming_Dexterity` 쪽이 자연스럽다. 이미 `MiniGameDef` 가 아는 값이다.

**내기가 아니라 응원이다.** 은이 오가지 않는다. 관전자가 마음속으로 한쪽을 고르고,
판이 끝나면 그 결과가 **생각 하나와 기록 한 줄**로 남는다. 그게 전부다.

| 있던 설계 | 바뀐 설계 |
|---|---|
| 판돈을 걸고 은을 주고받는다 | **아무것도 걸지 않는다** |
| 제로섬으로 묶어 총액을 지킨다 | 지킬 총액이 없다 |
| 슬롯 지갑 · `ColonySilver` 배관 | 배관이 아예 필요 없다 |
| 보상은 은 | 보상은 `ThoughtDef` 하나 |

**"오락이 환전소가 된다"는 위험이 관리 대상이 아니라 사라진다.** 앞 판에서 은을
제로섬으로 묶어야 한다고 적었던 것은, 은이 오간다는 전제 자체를 빼면 필요 없는 장치다.

- **응원은 그냥 고른다.** 관전을 시작할 때 한쪽이 정해진다 — 관계가 좋은 쪽, 같은
  이념, 아니면 그냥 무작위. 플레이어가 고르는 값이 아니다. **UI 를 늘리지 않는다.**
- **결과는 생각과 기록이다.** `PR_CheeredWinner` · `PR_CheeredLoser` 정도의 작은 무드.
  그리고 판이 끝날 때 18.1 의 서술 한 줄이 나간다 — *"○○가 응원하던 △△가 이겼다."*
- **그래서 18.1 과 한 몸이다.** 응원의 값은 그 한 줄이 남는 데 있다. 서술을 못 내보내면
  응원은 안 보이는 숫자가 되고, 그러면 만들 이유가 없다. **18.1 이 먼저다.**
- 승자에 대한 호감이 조금 오르는 것 정도는 얹어도 된다(`Pawn_RelationsTracker` 의 의견).
  다만 이것도 은과 달리 되돌릴 수 있는 값이라 부담이 없다.

**여전히 룰렛·슬롯과 한 식구다.** 카지노가 아니라 관중석 쪽으로 옮겨 앉았을 뿐이다.

---

## 19. 교차검증 대장 (2026-09-09)

Codex 교차검증 17건. **전부 실물로 다시 확인했다** — 디컴파일(`ilspycmd`)한
`Assembly-CSharp.dll`, 코어 Def 파일, 우리 소스. 확인되지 않은 지적은 받지 않았다.

### 뒤집힌 것 — 명세가 틀렸던 자리

| 어디 | 틀렸던 말 | 실제 |
|---|---|---|
| 18.3 | 관전 인원 상한이 `joyMaxParticipants` 로 공짜다 | `JobDriver_WatchBuilding` 이 **가구를 예약한다.** 참가자 둘이 `maxPawns: 2` 로 잡고 있으면 **관전자는 0명이다** |
| 18.1 | 모드를 빼면 `InteractionDef` 참조가 조용히 null 이 된다 | 참조는 null 이 되지만 **항목이 남고**, 표시 코드가 null 검사 없이 쓴다 |
| 18.2 | `GatheringDef` 는 Def 만 바꿔 재사용할 수 있다 | `GatheringWorker` 는 추상이고 결혼식 구현은 약혼에 묶여 있다. **세 벌을 새로 써야 한다** |
| 18.2 | `RitualBehaviorDef` 는 이데올로기 전용이다 | 이데올로기 12 · 로열티 2 · 바이오텍 1 · 오디세이 1. **코어에만 0개다** (결론은 같다) |
| D6 | 붙여 두면 바닐라가 대화를 굴린다 | 후보를 **같은 팩션에서만** 뽑는다. **방문객에게는 해당 없다** |
| D4 | 자리를 뜨면 판이 가구에 남는다 | `saveSessions` 를 끄면 지워진다. **설정 조건부였다** |
| 곁가지 | 그래브십은 `invalidateOnMove` 때문에 무효화된다 | 이륙 시 `DeSpawn(WillReplace)` → **설정 게이트보다 먼저 `BoardAlive` 에서 걸린다** |

### 빠져 있던 것 — 명세가 말하지 않던 자리

| 어디 | 무엇 |
|---|---|
| §4 | 기즈모(`pawn=null`)와 저장된 판 재개가 **진입 흐름을 통째로 우회한다** |
| §6.1 | 가구만 예약해서는 **같은 폰을 두 판의 상대로 지목**하는 것을 못 막는다 |
| §8 | `seatedPawn` 쪽 소멸은 여전히 검사하지 않는다 — 축은 "상대"가 아니라 "참가자"다 |
| §9 | 여가·숙련 스캐너가 `vanillaJob` 으로만 표를 만든다. **참가자는 아무것도 못 받는다** |
| 18.3 | "지금 두는 중"을 물을 데가 없다. `ActiveSession` 은 **하나뿐**이다 |
| §7.2 | `PlayLog` 폴링에 커서가 없다 — 창을 열면 옛 대화가 쏟아진다 |
| 18.1 | `TryInteractWith` 실패 조건이 쿨다운만이 아니다 (6칸 · 시야 · 각성 · 발화) |
| §9 · §10 | 바닐라 `Joy` NeedDef 가 `colonistsOnly` + `developmentalStageFilter: Adult` 라 **어린이·방문객·죄수·노예는 `needs.joy` 가 null 이다** |

### 덧붙여 확인한 것 (성격 · §10.2)

`NegativeInteractionUtility` 는 `Abrasive` 에 **×2.3**, `Kind` 에 **0** 을 준다(맨 앞에서
`return 0f`). `Aggression_HyperAggressive` 는 `socialFightChanceFactor: 3` 이다.
**성격 연출의 대부분은 붙잡기만 하면 따라온다** — 우리가 쓸 코드가 아니라 바닐라 값이다.
그리고 「몰입 모드」는 이미 출시된 설정 이름이라(`PR.Settings.Immersion`) 이 기능의
이름으로 쓸 수 없다.

### 살아남은 것

명세의 나머지 주장은 확인을 통과했다. `TryInteractWith` 는 공개 호출이고
(`bool TryInteractWith(Pawn, InteractionDef)`), `socialFightBaseChance` 를 비롯한
`InteractionDef` 필드는 전부 바닐라이며, `JoyGiver_WatchBuilding` · `JobDriver_WatchBuilding` ·
`WatchBuildingUtility` · `GatheringDef` 는 코어다. 림톡이 남의 상호작용을 읽는다는 것도
그대로다. **Harmony 없이 안 되는 항목은 하나도 나오지 않았다.**

### 순서에 미친 영향

1단계(상대 고르기)는 그대로 첫 조각이다. 다만 **§4 의 우회 경로와 §6.1 의 상대 예약이
1단계 안으로 들어온다** — 나중에 붙일 수 있는 것이 아니라, 이것 없이는 1단계가
"켰는데 어떤 길로는 그냥 열린다"가 되기 때문이다.

18.3(관전)은 "셋 중 가장 먼저 낼 수 있다"고 적었지만, **가구별 진행 중 레지스트리가
선행 조건으로 붙었다.** 그 값을 치르기 전에는 가장 싸지 않다.

---

## 곁가지 — 이 기능과 무관하게 확인할 것

**원인을 잘못 짚었다.** `invalidateOnMove` 가 아니다.

`Invalidation.Check` 는 맨 앞에서 `BoardAlive` 를 본다 — `board.Spawned && !board.Destroyed`.
그런데 그래브십은 이륙할 때 실려 가는 물건을 먼저 `DeSpawn(DestroyMode.WillReplace)`
한다. 그러면 `Spawned` 가 꺼지고, **설정 게이트에 닿기도 전에** `Destroyed` 로 무효화된다.
`invalidateSessions` 를 꺼 둬도 막히지 않는다 — 그 검사는 설정 위에 있다.

착륙 좌표도 `root + local` 이라 "좌표가 통째로 바뀐다"는 것도 늘 맞지는 않는다.
그러니까 이건 이동 감지 문제가 아니라 **"잠깐 사라졌다 돌아오는 것"을 파괴와 구별하지
못하는 문제다.** 지원하려면 `WillReplace` 상태를 따로 알아봐야 한다. 지금도 걸린다.
