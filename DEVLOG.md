# 개발 일지

무엇을 했는지는 `git log` 가 안다. 여기 적는 것은 **왜 그렇게 정했는지**,
그리고 **재 보니 얼마였는지**다. 커밋 메시지에 안 들어가는 것들.

---

## 2026-08-30 — 우르 한 게임으로 시작

`6ea0556`. 목표는 "기원전 2500년의 보드게임을 림월드 안에서 직접 두기".
이때 저장소 이름이 `RoyalGameOfUr` 였고, 그 이름이 네임스페이스까지 들어갔다.
하루 뒤에 그 결정이 비용이 된다.

처음부터 정한 것 두 가지가 끝까지 갔다.

- **Harmony 를 쓰지 않는다.** 진입점은 XML `PatchOperationAdd` 로 붙이는 `ThingComp` 하나
- **룰 엔진은 Verse 를 모른다.** 그래서 림월드 없이 테스트가 돈다

---

## 2026-08-31 — 하루 만에 일곱 게임

### 프레임워크를 뽑아내다 (`754a954`)

편자 던지기를 붙이려다 알았다 — 우르 코드의 절반은 우르에 대한 것이 아니었다.
창, 난이도, 전적, 튜토리얼, 저장, 무효화. 전부 "판을 두는 일"에 공통인 것들이다.

`Framework/` 로 뽑고 규칙을 세웠다. **프레임워크는 어떤 게임도 모르고, 게임끼리도
서로 모른다.** 편자와 후프스톤은 워커 하나에 Def 둘로 붙였다 — 규칙이 거의 같다.

여기서 배운 것: **턴·주사위·상대는 프레임워크의 개념이 아니다.** 나인볼에는 턴이 있지만
주사위가 없고, 망원경에는 상대가 없다. 프레임워크가 아는 것은 "창이 열리고, 워커가
그리고, 언젠가 끝난다" 뿐이어야 한다.

### 나인볼 — 규칙이 아니라 물리 (`87e21e4`)

처음으로 결과가 규칙표가 아니라 시뮬레이션에서 나오는 게임. 공은 실제로 굴러가고
쿠션에 튄다. AI 는 각도를 훑어 제일 나은 것을 고른다.

### 체스 — 증명하고 넣다 (`0a56b48`)

수 생성기는 **perft** 로 증명했다. 초기 위치와 함정 위치 몇 개에서 노드 수가
알려진 값과 정확히 일치해야 통과다. 이게 없으면 "앙파상에서만 틀리는" 버그를
사람 눈으로 찾아야 한다.

### 포커와 망원경 — 프레임워크가 승부를 놓다 (`1fa9a69`)

망원경에는 이기고 지는 것이 없다. 그런데 프레임워크는 모든 것에 승패가 있다고
믿고 있었다. `hasMatch` 를 두어 "승부가 아닌 항목"을 1급으로 만들었다.

별은 세계 시드가 정한다. 보이는 것은 정착지의 **실제 위도·경도**와 시각과 날씨가
정한다. 진짜 구면 천문 공식을 쓴다 — 국지 항성시, 고도·방위 변환, 황도 경사각.
값이 지구의 것이 아닐 뿐이다.

### 망원경, 궤도로 (`28d48bf`)

오디세이의 소행성·궤도 정거장 위에서는 하늘이 다르다. 행성이 하늘 한쪽을 차지하고,
망원경을 아래로 돌려 지표를 볼 수도 있다.

"서 있는 곳"을 `SkyPlace { Ground, Asteroid, Station }` 셋으로 나눴다.
**진공이냐만으로는 모자랐다** — 소행성과 정거장은 둘 다 진공이지만 한쪽은 발밑에
바위가 있어 하늘의 절반이 가려지고, 다른 쪽은 갑판뿐이라 구 전체가 보인다.

### 코드 리뷰가 찾은 여섯 (`c27d1db`)

**체스의 프레임 예산 — 추정하지 말고 재라.**
리뷰어는 "한 프레임에 너무 오래 걸린다"고 했다. 임시 계측을 붙여 재 봤더니
**331 ms**. 추정보다 나빴다. 탐색을 루트 수 단위로 쪼개 프레임에 나눠 담고
다시 쟀더니 **25 ms**. 덤으로, 전에는 깊이 4 에서 포기하던 자리에서 5 까지 판다.
계측 파일은 지웠다.

**포커의 세이브스컴 구멍.** `Actions` 가 봇 판단의 시드로 들어가는데 그걸 저장하지
않고 있었다. 창을 닫았다 열면 상대의 결정이 다시 굴러갔다. 저장 대상에 넣었다.

편자도 같은 이유로 **이닝 중간 상태를 저장**하기로 했다. 이닝을 되감는 편이 코드는
쉽지만, 그러면 다른 종류의 세이브스컴이 열린다.

**`HandEval.BestFive` 는 자기 검사를 한다.** 스물한 가지를 전부 세는 무식한 방법을
골랐다. 규칙을 거꾸로 되짚는 것보다 짧고, 무엇보다 `Score` 와 어긋날 수가 없다.
무작위 7 장 3,000 판에서 둘이 일치하는지 테스트가 확인한다.

### 폴리싱 (`5c8b153`)

인게임에서 최소한 돌아가는 것을 확인한 뒤. 문구를 사람 말로, 무슨 일이 일어났는지
보이게. 우르에 직전 수의 자취를, 포커 쇼다운에 이긴 다섯 장 테두리를 넣었다.

`Tools/guistate.py` 를 썼다 — `Text.Anchor` / `Text.Font` / `GUI.color` 를 바꿔 놓고
안 되돌리는 메서드를 찾는다. 그 프레임 전체가 공유하는 상태라, 한 곳에서 흘리면
그 다음에 그려지는 남의 창 글자가 틀어진다. 눈으로는 "가끔 이상해 보인다"로만 나타난다.
93 파일 · 643 메서드 중 60 개가 건드리는데, 전부 되돌리고 있었다.

---

## 2026-08-31 밤 — 배포까지

### 저자 이름 (`488f461`)

`tr8425` → `Team Rostra`. packageId 도 `teamrostra.playablerecreation` 로.
개인 메일 주소가 모드 저자에 안 보이게 하는 것이 목적이었다. 커밋 이메일은
창작마당에 안 올라가므로 그대로 뒀다 — **단, `.git` 을 통째로 올리지 않는다는
전제 하에서만 그렇다.** 이 전제가 나중에 `Tools/package.py` 의 이유가 된다.

### 체스 기물이 안 보였다 (`488f461`)

인게임 스크린샷에서 체스판은 있는데 기물이 없었다. 포커 카드는 무늬가 없었다.
**오류 로그는 한 줄도 없었다.**

`Player.log` 에 `null texture passed to GUI.DrawTexture` 가 **5087 줄** 있었다.
체스는 널을 걸러 내고 `return` 해서 조용히 빈 판을 그렸고, 포커는 안 걸러서
로그에 쌓인 것이다. 같은 병, 다른 증상.

둘 다 `ContentFinder<Texture2D>.Get` 결과를 정적 필드에 한 번 담아 두는 방식이었다.
PNG 는 디스크에 멀쩡히 있고(128×128 RGBA, 알파 실루엣 정상), 심볼릭 링크도 살아 있고,
ContentFinder 실패 로그도 없었다. **불러오기는 됐는데 그리는 시점엔 널이었다.**

정확히 누가 죽였는지는 못 짚었다. 그래서 **원인 특정에 기대지 않는 방식**으로 고쳤다
(`Framework/UI/PRContent.cs`) — 그릴 때마다 살아 있는지 보고, 죽었으면 다시 굽는다.
ContentFinder 가 실패하면 모드 폴더에서 직접 읽고, 그래도 없으면 글자라도 세운다.
못 찾으면 경고를 **딱 한 줄** 남긴다.

> 남은 숙제: 근본 원인은 여전히 모른다. 다시 재현되면
> `PRContent` 의 경고 한 줄이 모드 폴더 경로와 함께 단서를 줄 것이다.

### 스페이스를 돌려주다 (`a574344`)

판을 진행시키는 키가 스페이스였다. 그런데 스페이스는 콜로니를 멈추는 키다.
창이 `absorbInputAroundWindow = false` 라 — 뒤쪽 콜로니를 계속 보려고 그렇게 뒀다 —
우리가 키를 삼키지 못하는 순간마다(상대 차례, 굴러가는 중) 그대로 일시정지로 샜다.
"게임 속도를 건드리지 않는다"는 약속이 그렇게 깨지고 있었다.

바닐라 키바인딩 61 개를 전수로 뽑아 봤다. **A–Z 스물여섯 글자가 전부 할당돼 있다.**
남은 것은 F1–F9, 숫자 5~0, Backspace, Delete, 그리고 `Return`.

엔터로 옮겼다. `Return` 은 바닐라에서 `Accept` — UI 확인 키일 뿐 세계에는 아무 일도
하지 않는다. 창이 `closeOnAccept = false` 라 눌러도 닫히지 않는다.
(바닐라 `Dialog_Rename` 이 쓰는 것과 같은 패턴이라 안전을 확인할 수 있었다.)

### 소행성이 한 덩어리로 떠 있었다 (`a574344`)

스물한 개가 북쪽 지평선 한 자락에 겹쳐 있었다. 자리를 타일 번호의 해시에서 뽑는데,
궤도 타일 번호는 서로 이웃한 값이고 `Gen.HashCombineInt` 는 이웃한 입력에 이웃한
출력을 준다. 특히 **위쪽 비트가 거의 안 움직여서 적위가 스물한 개 전부 같은 값**이었다.

연속 타일 21 개로 재 봤다.

| | 뭉침도 R | 적경 폭 | 적위 폭 |
|---|---|---|---|
| `Gen.HashCombineInt` | **1.000** | 10.2° | **0.0°** |
| `SkyMath.Scatter` | 0.219 | 329.7° | 106.8° |

lowbias32 파이널라이저를 `SkyMath.Scatter` 로 넣었다. 무작위가 아니라 섞기다 —
같은 타일은 언제나 같은 자리를 받는다. 회귀 테스트 4 개.

### 배포 준비 (`75ff9c3`)

**림월드 업로더는 모드 폴더를 통째로 올린다** (`SetItemContent(RootDir)`).
이 저장소가 곧 모드 폴더이므로, 링크된 채로 올리면 `.git`(3.3 MB) 과
`Tests/`(11 MB) 가 따라 올라간다. `.git` 에는 커밋 기록이 들어 있으니
저자 이름을 바꾼 의미가 없어진다.

올릴 것만 담으니 **432 KB · 36 파일**. `Tools/package.py` 가 굽는다.
굽기 전 검사 중 제일 값어치가 있는 것은 **"DLL 이 어느 `.cs` 보다도 새것인가"** 다 —
빌드를 잊고 올리는 것이 제일 흔한 사고다.

설명은 영어를 앞으로 돌렸다. 창작마당에서 이 모드를 처음 보는 사람의 언어가 그쪽이다.

### 이름을 하나로 (`63ba715`)

우르의 네임스페이스가 아직 `RoyalGameOfUr` 였다. 형제들은 `Chess` · `Poker` ·
`Billiards` · `Throwing` · `Stargazing` 인데 혼자만 저장소 이름을 쓰고 있었다.

**`Ur` 로 맞췄다. `PlayableRecreation` 이 아니다** — 그건 프레임워크의 이름이고,
게임 하나가 프레임워크와 같은 이름을 쓰면 층이 무너진다.

타이밍이 중요했다. `Scribe_Deep` 이 세이브에 `Class="Ur.UrSaveData"` 를 그대로 박는다.
**올린 뒤에 바꾸면 구독자가 두던 판이 사라진다.** 배포 직전이 마지막 기회였다.

덤으로 `Tools/verify.py` 와 `make_art.py` 가 저장소 절대경로를 박아 두고 있었다.
폴더 이름을 바꾸면 바로 깨질 것들이라 `__file__` 기준으로 스스로 찾게 했다.

### 썸네일 (`171618c`)

한글을 영어로. 요약 칸의 마지막 줄이 "Harmony 패치 없음" 이었는데 맨 아래 띠가
이미 같은 말을 하고 있었다. **DLC 가 필요 없다**는 사실로 바꿨다. 그게 더 궁금한 것이다.

---

## 2026-09-01 — 난이도 이름을 품질 등급으로

Novice~Master 는 어느 게임에나 있는 이름이다. 림월드 플레이어가 매일 보는
사다리는 따로 있다 — 장비 품질 일곱 등급. 그래서 다섯 난이도를 품질 이름으로 바꿨다:
**빈약 / 평범 / 상급 / 완벽 / 걸작** (Poor / Normal / Good / Excellent / Masterwork).

일곱 중 둘은 자리를 비워 뒀는데, 이게 핵심이다.

- **끔찍(Awful)** 은 연습 판이 가져갔다. 체크박스 문구가 "끔찍한 상대로 연습하기"다.
  기록에 안 남는 최하 단계니까 이름값을 한다
- **전설(Legendary)** 은 일부러 안 쓴다. 나중에 무한 난이도나 네트워크 대전이 생기면
  그 자리다. AI 일곱 단계로 늘리는 것도 검토했지만(프레임워크는 이미
  `difficultyCount` 변수 기반이라 가능은 하다), 게임당 AI 곡선을 두 단계씩
  더 튜닝하는 비용 대비 다섯 단계 유지 + 양끝 예약이 나았다

숙련도 21단계 이름(초보자~전설)도 후보였지만, 다섯 개만 뽑으면 사다리가
듬성해지고 영어명이 길다 (Solid professional). 품질 쪽이 다섯에 딱 맞았다.

---

## 아직 안 한 것

- 창작마당 업로드 (`Workshop/RELEASE.md`)
- 스크린샷 3장 — 편자 플레이 창, 오디세이 궤도 하늘, 모드 설정 창
- 별 보기 라벨 겹침 — 자리는 퍼졌지만 이름표는 여전히 겹칠 수 있다
